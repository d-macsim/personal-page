---
phase: 04-projects-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.ts
  - src/data/contact.ts
  - src/components/ProjectCard.astro
  - src/components/ProjectsSection.astro
  - src/components/ContactSection.astro
  - src/components/AboutSection.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - CONT-01

must_haves:
  truths:
    - "A projects section is visible on the page with a mytai card showing description, tech stack badges, and a link to mytai.uk"
    - "A contact section is visible below projects with email, LinkedIn, and GitHub icon links"
    - "The projects grid structure renders from a typed data array and can accept additional cards without layout changes"
    - "AboutSection has id=about so nav anchors will work in Plan 02"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "Typed project content array with mytai entry"
      exports: ["projects", "Project", "TechBadge"]
      contains: "as const satisfies"
    - path: "src/data/contact.ts"
      provides: "Typed contact links array"
      exports: ["contactLinks", "ContactLink"]
      contains: "as const satisfies"
    - path: "src/components/ProjectCard.astro"
      provides: "Feature card with device frame and project details"
      contains: "mytai app preview"
    - path: "src/components/ProjectsSection.astro"
      provides: "Section wrapper with grid container"
      contains: "id=projects"
    - path: "src/components/ContactSection.astro"
      provides: "Contact CTA section with icon links"
      contains: "id=contact"
    - path: "src/pages/index.astro"
      provides: "Page integration of new sections"
      contains: "ProjectsSection"
  key_links:
    - from: "src/components/ProjectsSection.astro"
      to: "src/data/projects.ts"
      via: "import { projects }"
      pattern: "import.*projects.*from.*data/projects"
    - from: "src/components/ContactSection.astro"
      to: "src/data/contact.ts"
      via: "import { contactLinks }"
      pattern: "import.*contactLinks.*from.*data/contact"
    - from: "src/pages/index.astro"
      to: "ProjectsSection and ContactSection"
      via: "component imports"
      pattern: "ProjectsSection|ContactSection"
---

<objective>
Create the mytai project showcase card, contact CTA section, and integrate both into the page. This plan delivers all visible content for Phase 4 — the sections that users will see and interact with.

Purpose: Completes the site's browsable content (projects + contact) before navigation is wired up in Plan 02.
Output: ProjectsSection with mytai feature card, ContactSection with icon links, both rendered on the page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-projects-contact/04-CONTEXT.md
@.planning/phases/04-projects-contact/04-RESEARCH.md
@.planning/phases/04-projects-contact/04-UI-SPEC.md

@src/data/cv.ts
@src/components/SkillsGrid.astro
@src/components/CVDownloadButton.astro
@src/components/AboutSection.astro
@src/pages/index.astro
@src/styles/global.css

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/data/cv.ts (pattern to mirror):
```typescript
export interface SkillCategory {
  category: string;
  skills: string[];
}
export const roles: readonly Role[] = [ ... ] as const satisfies readonly Role[];
```

From src/components/SkillsGrid.astro (badge chip pattern):
```astro
<span
  class="px-3 py-1 rounded-full text-sm font-medium"
  style="background-color: var(--color-surface); border: 1px solid var(--color-border); color: var(--color-text);"
>
  {skill}
</span>
```

From src/components/CVDownloadButton.astro (CTA button pattern):
```astro
<a
  class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold transition-colors duration-200"
  style="background-color: var(--color-accent-primary); color: white;"
>
```

From src/components/CVSection.astro (section wrapper pattern):
```astro
<section id="experience" aria-label="Experience and CV" class="py-16 md:py-24">
  <h2 class="font-semibold mb-12" style={{ fontSize: "var(--font-size-heading)" }}>
```

From src/pages/index.astro (current page structure):
```astro
<BaseLayout title="Dragos Macsim -- AI Specialist & Product Builder">
  <HeroSection client:load />
  <AboutSection />
  <CVSection />
</BaseLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create projects and contact data files, fix AboutSection id</name>
  <files>src/data/projects.ts, src/data/contact.ts, src/components/AboutSection.astro</files>
  <read_first>
    - src/data/cv.ts (pattern to mirror for typed data with as const satisfies)
    - src/components/AboutSection.astro (to add id="about" attribute)
  </read_first>
  <action>
**1. Create `src/data/projects.ts`** — typed project content array mirroring cv.ts immutability pattern.

Define these exact interfaces and data:

```typescript
// src/data/projects.ts — Typed project content
// All project data lives here; components only render structure.

export interface TechBadge {
  readonly label: string;
}

export interface Project {
  readonly id: string;
  readonly title: string;
  readonly tagline: string;
  readonly description: string;
  readonly bullets: readonly string[];
  readonly techStack: readonly TechBadge[];
  readonly url: string;
  readonly urlLabel: string;
}

export const projects: readonly Project[] = [
  {
    id: "mytai",
    title: "mytai",
    tagline: "Your AI personal trainer",
    description:
      "An all-in-one AI personal trainer app built with React Native and Expo. Combat sports form analysis using on-device AI, gym workout logging, calorie tracking, weight tracking, and mobility exercises — privacy-first with on-device processing.",
    bullets: [
      "On-device AI form analysis for combat sports — no data leaves your device",
      "Full workout logging: gym sessions, calorie tracking, weight tracking",
      "iOS available now — Android coming soon",
    ],
    techStack: [
      { label: "React Native" },
      { label: "Expo" },
      { label: "On-device AI" },
      { label: "TypeScript" },
      { label: "iOS" },
    ],
    url: "https://mytai.uk",
    urlLabel: "Visit mytai.uk",
  },
] as const satisfies readonly Project[];
```

**2. Create `src/data/contact.ts`** — typed contact links array.

```typescript
// src/data/contact.ts — Typed contact link content
// All contact data lives here; components only render structure.

export interface ContactLink {
  readonly label: string;
  readonly href: string;
  readonly isExternal: boolean;
  readonly iconType: "email" | "linkedin" | "github";
}

export const contactLinks: readonly ContactLink[] = [
  {
    label: "Email",
    href: "mailto:dragosmacsim@outlook.com",
    isExternal: false,
    iconType: "email",
  },
  {
    label: "LinkedIn",
    href: "https://www.linkedin.com/in/dragos-macsim/",
    isExternal: true,
    iconType: "linkedin",
  },
  {
    label: "GitHub",
    href: "https://github.com/dragos-macsim",
    isExternal: true,
    iconType: "github",
  },
] as const satisfies readonly ContactLink[];
```

Note: The email and social URLs above are placeholder values. The user should update them with real URLs before deployment. Use these exact placeholder values so tests can assert on structure.

**3. Fix `src/components/AboutSection.astro`** — add `id="about"` to the section element.

Change the opening `<section>` tag from:
```
<section aria-label="About" class="py-16 md:py-24">
```
to:
```
<section id="about" aria-label="About" class="py-16 md:py-24">
```

No other changes to AboutSection.
  </action>
  <verify>
    <automated>grep -q 'as const satisfies' src/data/projects.ts && grep -q 'as const satisfies' src/data/contact.ts && grep -q 'id="about"' src/components/AboutSection.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/projects.ts contains `export interface Project`
    - src/data/projects.ts contains `export const projects: readonly Project[]`
    - src/data/projects.ts contains `as const satisfies readonly Project[]`
    - src/data/projects.ts contains `title: "mytai"`
    - src/data/projects.ts contains `url: "https://mytai.uk"`
    - src/data/contact.ts contains `export interface ContactLink`
    - src/data/contact.ts contains `export const contactLinks: readonly ContactLink[]`
    - src/data/contact.ts contains `as const satisfies readonly ContactLink[]`
    - src/data/contact.ts contains `iconType: "email"`
    - src/data/contact.ts contains `iconType: "linkedin"`
    - src/data/contact.ts contains `iconType: "github"`
    - src/components/AboutSection.astro contains `id="about"`
  </acceptance_criteria>
  <done>projects.ts and contact.ts exist with fully typed immutable data arrays. AboutSection has id="about" for nav anchor linking.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectCard, ProjectsSection, ContactSection components and wire into index.astro</name>
  <files>src/components/ProjectCard.astro, src/components/ProjectsSection.astro, src/components/ContactSection.astro, src/pages/index.astro</files>
  <read_first>
    - src/data/projects.ts (data shape to render)
    - src/data/contact.ts (data shape to render)
    - src/components/SkillsGrid.astro (badge chip pattern to replicate for tech stack per D-03)
    - src/components/CVDownloadButton.astro (CTA button pattern to replicate per D-04)
    - src/components/CVSection.astro (section wrapper pattern)
    - src/pages/index.astro (current page structure to extend)
    - src/styles/global.css (design tokens for inline styles)
    - .planning/phases/04-projects-contact/04-UI-SPEC.md (visual contracts)
  </read_first>
  <action>
**1. Create `src/components/ProjectCard.astro`** — feature card with device frame placeholder (D-01, D-02, D-03, D-04).

Structure:
- Accept a `project` prop typed as `Project` from `../data/projects`
- Layout: `flex flex-col md:flex-row gap-8 md:gap-12 items-center md:items-start`
- Card wrapper: `p-6 md:p-8 rounded-2xl` with `background-color: var(--color-surface); border: 1px solid var(--color-border);`

Left side — device frame placeholder (D-02):
- Container: `w-full max-w-[240px] mx-auto md:mx-0 md:flex-shrink-0 md:w-[240px] h-[480px] rounded-[2.5rem] overflow-hidden relative`
- Border: `border: 2px solid var(--color-border); background: var(--color-surface);`
- `aria-label="mytai app preview"`
- Inner gradient overlay (aria-hidden="true"): `absolute inset-0` with `background: radial-gradient(ellipse at 30% 40%, rgba(99,102,241,0.4) 0%, transparent 65%), radial-gradient(ellipse at 70% 70%, rgba(245,158,11,0.25) 0%, transparent 60%);`
- Brand text overlay: `absolute inset-0 flex items-center justify-center` with `<span class="font-bold tracking-tight" style="font-size: 2rem; color: var(--color-text);">mytai</span>`

Right side — project details:
- Title `<h3>` with `font-size: var(--font-size-heading); font-weight: 600;`
- Tagline `<p>` with `color: var(--color-text-muted); font-size: var(--font-size-body);`
- Description `<p>` with `color: var(--color-text); font-size: var(--font-size-body); margin-top: 1rem;`
- Feature bullets as `<ul>` with `list-disc pl-5 mt-4 space-y-2` each `<li>` styled with `color: var(--color-text); font-size: var(--font-size-body);`
- Tech stack badges: `flex flex-wrap gap-2 mt-6` — map `project.techStack` to `<span class="px-3 py-1 rounded-full text-sm font-medium" style="background-color: var(--color-surface); border: 1px solid var(--color-border); color: var(--color-text);">{badge.label}</span>` (exact SkillsGrid pattern per D-03)
- CTA button: `<a href={project.url} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold transition-colors duration-200 mt-6" style="background-color: var(--color-accent-primary); color: white;">{project.urlLabel}</a>` with external link arrow SVG icon (per D-04, D-12)

Add a `<style>` block for hover/active states on the CTA button (same pattern as CVDownloadButton):
```css
a.project-cta:hover {
  background-color: var(--color-accent-primary-hover) !important;
}
a.project-cta:active {
  background-color: var(--color-accent-primary-press) !important;
}
```
Add `class="project-cta"` to the CTA `<a>` tag to scope the styles.

**2. Create `src/components/ProjectsSection.astro`** — section wrapper with grid (D-05, PROJ-02).

```astro
  ---
import ProjectCard from "./ProjectCard.astro";
import { projects } from "../data/projects";
  ---

<section id="projects" aria-label="Projects" class="py-16 md:py-24">
  <div class="max-w-[1100px] mx-auto">
    <h2
      class="font-semibold mb-12"
      style={{ fontSize: "var(--font-size-heading)" }}
    >
      Projects
    </h2>
    <div class="grid grid-cols-1 gap-8">
      {projects.map((project) => (
        <ProjectCard project={project} />
      ))}
    </div>
  </div>
</section>
```

The `grid grid-cols-1 gap-8` container maps over the data array — adding a second project to `projects.ts` automatically renders a second card (PROJ-02).

**3. Create `src/components/ContactSection.astro`** — contact CTA section (D-10, D-11, D-12, D-13, CONT-01).

Center-aligned layout with heading, message, and icon links.

Import `contactLinks` from `../data/contact`.

Section structure:
```astro
<section id="contact" aria-label="Contact" class="py-16 md:py-24">
  <div class="max-w-[1100px] mx-auto text-center">
    <h2 class="font-semibold mb-4" style={{ fontSize: "var(--font-size-heading)" }}>
      Get in Touch
    </h2>
    <p class="mb-12" style={{ color: "var(--color-text-muted)", fontSize: "var(--font-size-body)" }}>
      Open to new opportunities, collaborations, and conversations. Reach out on any of the channels below.
    </p>
    <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center">
      {contactLinks.map((link) => (
        <a
          href={link.href}
          target={link.isExternal ? "_blank" : undefined}
          rel={link.isExternal ? "noopener noreferrer" : undefined}
          class="contact-link inline-flex items-center justify-center gap-3 px-6 py-4 rounded-lg transition-colors duration-200"
          style={{
            backgroundColor: "var(--color-surface)",
            border: "1px solid var(--color-border)",
            color: "var(--color-text)",
          }}
          aria-label={`${link.label}${link.isExternal ? " (opens in new tab)" : ""}`}
        >
          <!-- Icon rendered by iconType -->
          <span>{link.label}</span>
        </a>
      ))}
    </div>
  </div>
</section>
```

For icons, render inline SVGs based on `link.iconType`. Use a helper function or conditional rendering:
- `"email"`: Envelope icon — `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 7l9 6 9-6"/><rect x="3" y="5" width="18" height="14" rx="2"/></svg>`
- `"linkedin"`: LinkedIn logo — `<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>`
- `"github"`: GitHub logo — `<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"/></svg>`

Add a `<style>` block for hover state (per UI-SPEC interaction contract):
```css
.contact-link:hover {
  border-color: var(--color-accent-primary) !important;
  color: var(--color-accent-primary) !important;
}
```

**4. Update `src/pages/index.astro`** — add imports and render new sections after CVSection.

Add imports:
```
import ProjectsSection from "../components/ProjectsSection.astro";
import ContactSection from "../components/ContactSection.astro";
```

Add after `<CVSection />`:
```
<ProjectsSection />
<ContactSection />
```

Final page order: HeroSection > AboutSection > CVSection > ProjectsSection > ContactSection.
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npx astro check 2>&1 | tail -5 && grep -q 'id="projects"' src/components/ProjectsSection.astro && grep -q 'id="contact"' src/components/ContactSection.astro && grep -q 'ProjectsSection' src/pages/index.astro && grep -q 'ContactSection' src/pages/index.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ProjectCard.astro exists and contains `aria-label="mytai app preview"`
    - src/components/ProjectCard.astro contains `radial-gradient(ellipse at 30% 40%, rgba(99,102,241,0.4)`
    - src/components/ProjectCard.astro contains `rounded-full text-sm font-medium` (badge chip class)
    - src/components/ProjectCard.astro contains `target="_blank"` and `rel="noopener noreferrer"`
    - src/components/ProjectCard.astro contains `var(--color-accent-primary)`
    - src/components/ProjectsSection.astro contains `id="projects"`
    - src/components/ProjectsSection.astro contains `grid grid-cols-1 gap-8`
    - src/components/ProjectsSection.astro contains `import { projects } from`
    - src/components/ContactSection.astro contains `id="contact"`
    - src/components/ContactSection.astro contains `Get in Touch`
    - src/components/ContactSection.astro contains `rel="noopener noreferrer"` (for external links)
    - src/components/ContactSection.astro contains `mailto:` (for email link)
    - src/components/ContactSection.astro contains `aria-hidden="true"` (on SVG icons)
    - src/pages/index.astro contains `import ProjectsSection`
    - src/pages/index.astro contains `import ContactSection`
    - src/pages/index.astro contains `<ProjectsSection />` after `<CVSection />`
    - src/pages/index.astro contains `<ContactSection />` after `<ProjectsSection />`
  </acceptance_criteria>
  <done>ProjectCard renders mytai with device frame, tech badges, and CTA button. ProjectsSection wraps in a scalable grid. ContactSection shows email/LinkedIn/GitHub icon links. Both sections render on the page after CVSection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page -> external URLs | mytai.uk link and social profile links open in new tabs |
| page -> mailto handler | Email link triggers OS mail client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ProjectCard CTA link (target="_blank") | mitigate | Add `rel="noopener noreferrer"` on all external links per D-12 — prevents opened tab from accessing window.opener |
| T-04-02 | Tampering | ContactSection LinkedIn/GitHub links (target="_blank") | mitigate | Add `rel="noopener noreferrer"` on all external links per D-12 |
| T-04-03 | Information Disclosure | Contact email in mailto: link | accept | Email address is intentionally public on a portfolio site; no obfuscation needed |
| T-04-04 | Tampering | Nav anchor hrefs | accept | All hrefs are same-page `#anchor` format or hardcoded external URLs — no user-controlled redirect vector |
</threat_model>

<verification>
- `npx astro check` completes with no errors
- `npx vitest run` passes all existing tests (162+)
- `npx astro dev` renders ProjectsSection and ContactSection on the page
- Hero "View my work" button scrolls to the projects section (id="projects" matches href="#projects")
</verification>

<success_criteria>
- mytai project card visible with device frame placeholder, description, tech badges, and working CTA link to mytai.uk
- Contact section visible with email, LinkedIn, and GitHub icon links
- All external links have target="_blank" rel="noopener noreferrer"
- Email link uses mailto: scheme
- Projects grid renders from data array (adding a second entry to projects.ts would render a second card)
- Page order: Hero > About > CV > Projects > Contact
</success_criteria>

<output>
After completion, create `.planning/phases/04-projects-contact/04-01-SUMMARY.md`
</output>
