---
phase: 04-projects-contact
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/layouts/BaseLayout.astro
  - tests/projects-contact.test.ts
autonomous: false
requirements:
  - CONT-02
  - PROJ-01
  - PROJ-02
  - CONT-01

must_haves:
  truths:
    - "Nav bar shows section anchor links (About, Experience, Projects, Contact) on desktop"
    - "Hamburger menu collapses section links on mobile with ThemeToggle still visible"
    - "Clicking a nav link smooth-scrolls to the target section"
    - "Active section is highlighted in the nav as user scrolls"
    - "All Phase 4 tests pass alongside existing 162+ tests"
  artifacts:
    - "src/layouts/BaseLayout.astro contains data-nav-link, IntersectionObserver, hamburger menu"
    - "tests/projects-contact.test.ts covers PROJ-01, PROJ-02, CONT-01, CONT-02"
  key_links:
    - "BaseLayout Intersection Observer observes all section[id] elements"
    - "Nav anchor hrefs point to #about, #experience, #projects, #contact"
    - "tests/projects-contact.test.ts uses static file analysis (readFileSync)"
---

<objective>
Upgrade the nav bar with section anchor links, hamburger menu for mobile, Intersection Observer active state, and smooth scroll. Then create the test file covering all Phase 4 requirements. Concludes with a visual verification checkpoint.

Purpose: Makes contact reachable from any scroll position (CONT-02) and validates all Phase 4 work with automated tests.
Output: Fully navigable sticky nav and comprehensive test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-projects-contact/04-CONTEXT.md
@.planning/phases/04-projects-contact/04-RESEARCH.md
@.planning/phases/04-projects-contact/04-UI-SPEC.md
@.planning/phases/04-projects-contact/04-01-SUMMARY.md

@src/layouts/BaseLayout.astro
@src/components/ThemeToggle.astro
@tests/cv-section.test.ts

<interfaces>
<!-- Current BaseLayout.astro nav structure (to be upgraded): -->
```astro
<nav class="fixed top-0 right-0 left-0 z-50 bg-base/80 backdrop-blur-sm border-b border-border transition-colors duration-300 ease-in-out">
  <div class="max-w-[1100px] mx-auto px-4 md:px-8 lg:px-12 flex items-center justify-end h-14">
    <ThemeToggle />
  </div>
</nav>
<main class="max-w-[1100px] mx-auto px-4 md:px-8 lg:px-12 pt-20">
```

<!-- ThemeToggle.astro script pattern (established for Astro interactivity): -->
```astro
<script>
  // Scoped to component — bundled once — no React needed
  const btn = document.getElementById('...');
  btn?.addEventListener('click', () => { ... });
</script>
```

<!-- Test pattern from tests/cv-section.test.ts: -->
```typescript
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { join } from "node:path";

const ROOT = join(import.meta.dirname, "..");
const readFile = (rel: string) => readFileSync(join(ROOT, rel), "utf-8");
```

<!-- Section IDs available after Plan 01: -->
- id="about" on AboutSection.astro
- id="experience" on CVSection.astro
- id="projects" on ProjectsSection.astro
- id="contact" on ContactSection.astro
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade BaseLayout nav with section links, hamburger menu, Intersection Observer, and smooth scroll</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (current nav structure — must preserve ThemeToggle, existing classes)
    - src/components/ThemeToggle.astro (script pattern for Astro interactivity)
    - src/components/HeroSection.tsx (lines 73-79 — scrollIntoView pattern to match, do NOT add global scroll-behavior: smooth)
    - .planning/phases/04-projects-contact/04-UI-SPEC.md (interaction contracts for nav states)
    - .planning/phases/04-projects-contact/04-RESEARCH.md (Pattern 4: nav structure and Intersection Observer)
  </read_first>
  <action>
Rewrite `src/layouts/BaseLayout.astro` to upgrade the nav from right-only ThemeToggle to a full navigation component. Keep ALL existing functionality (ThemeToggle, body classes, Head component, main wrapper).

**Nav HTML structure (per D-06, D-07):**

Replace the current `<nav>` inner div with:

```astro
<nav aria-label="Main navigation" class="fixed top-0 right-0 left-0 z-50 bg-base/80 backdrop-blur-sm border-b border-border transition-colors duration-300 ease-in-out">
  <div class="max-w-[1100px] mx-auto px-4 md:px-8 lg:px-12 flex items-center justify-between h-14">
    <!-- Left: empty spacer for layout balance (Claude's discretion: no logo/name) -->
    <div class="hidden md:block"></div>

    <!-- Center-right: desktop nav links (hidden on mobile) -->
    <div class="hidden md:flex items-center gap-6">
      <a href="#about" data-nav-link class="nav-link text-sm font-medium transition-colors duration-200" style="color: var(--color-text-muted);">About</a>
      <a href="#experience" data-nav-link class="nav-link text-sm font-medium transition-colors duration-200" style="color: var(--color-text-muted);">Experience</a>
      <a href="#projects" data-nav-link class="nav-link text-sm font-medium transition-colors duration-200" style="color: var(--color-text-muted);">Projects</a>
      <a href="#contact" data-nav-link class="nav-link text-sm font-medium transition-colors duration-200" style="color: var(--color-text-muted);">Contact</a>
    </div>

    <!-- Right: ThemeToggle + hamburger (mobile only) -->
    <div class="flex items-center gap-2">
      <ThemeToggle />
      <button
        id="hamburger-btn"
        class="md:hidden flex items-center justify-center w-11 h-11 rounded-lg transition-colors duration-200"
        aria-label="Open navigation menu"
        aria-expanded="false"
      >
        <svg id="hamburger-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <line x1="3" y1="6" x2="21" y2="6"/>
          <line x1="3" y1="12" x2="21" y2="12"/>
          <line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
        <svg id="close-icon" class="hidden" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <line x1="18" y1="6" x2="6" y2="18"/>
          <line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile dropdown menu (hidden by default) per D-07 -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-border" style="background-color: var(--color-base);">
    <div class="max-w-[1100px] mx-auto px-4 py-2">
      <a href="#about" data-nav-link class="mobile-nav-link block py-3 px-4 text-sm font-medium rounded-lg transition-colors duration-200" style="color: var(--color-text-muted);">About</a>
      <a href="#experience" data-nav-link class="mobile-nav-link block py-3 px-4 text-sm font-medium rounded-lg transition-colors duration-200" style="color: var(--color-text-muted);">Experience</a>
      <a href="#projects" data-nav-link class="mobile-nav-link block py-3 px-4 text-sm font-medium rounded-lg transition-colors duration-200" style="color: var(--color-text-muted);">Projects</a>
      <a href="#contact" data-nav-link class="mobile-nav-link block py-3 px-4 text-sm font-medium rounded-lg transition-colors duration-200" style="color: var(--color-text-muted);">Contact</a>
    </div>
  </div>
</nav>
```

Keep `<main>` wrapper unchanged: `class="max-w-[1100px] mx-auto px-4 md:px-8 lg:px-12 pt-20"`.

**Script block** — add a `<script>` block inside `<body>` before the closing `</body>` tag:

```html
<script>
  // Hamburger menu toggle
  const hamburgerBtn = document.getElementById('hamburger-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const hamburgerIcon = document.getElementById('hamburger-icon');
  const closeIcon = document.getElementById('close-icon');

  hamburgerBtn?.addEventListener('click', () => {
    const isOpen = !mobileMenu?.classList.contains('hidden');
    mobileMenu?.classList.toggle('hidden', isOpen);
    hamburgerIcon?.classList.toggle('hidden', !isOpen);
    closeIcon?.classList.toggle('hidden', isOpen);
    hamburgerBtn.setAttribute('aria-expanded', String(!isOpen));
  });

  // Close mobile menu when a link is clicked
  const mobileLinks = document.querySelectorAll('#mobile-menu a');
  mobileLinks.forEach((link) => {
    link.addEventListener('click', () => {
      mobileMenu?.classList.add('hidden');
      hamburgerIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
      hamburgerBtn?.setAttribute('aria-expanded', 'false');
    });
  });

  // Smooth scroll for all nav links (per D-09 — use scrollIntoView, NOT global CSS scroll-behavior)
  const allNavLinks = document.querySelectorAll('[data-nav-link]');
  allNavLinks.forEach((link) => {
    link.addEventListener('click', (e) => {
      e.preventDefault();
      const href = link.getAttribute('href');
      if (href) {
        const target = document.querySelector(href);
        target?.scrollIntoView({ behavior: 'smooth' });
      }
    });
  });

  // Intersection Observer for active section highlighting (per D-08)
  const sections = document.querySelectorAll('section[id]');
  const navLinks = document.querySelectorAll('[data-nav-link]');

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          navLinks.forEach((navLink) => {
            const href = navLink.getAttribute('href');
            if (href === `#${entry.target.id}`) {
              navLink.setAttribute('data-active', 'true');
            } else {
              navLink.removeAttribute('data-active');
            }
          });
        }
      });
    },
    { rootMargin: '-40% 0px -55% 0px', threshold: 0 }
  );

  sections.forEach((section) => observer.observe(section));
</script>
```

**Style block** — add a `<style is:global>` block for nav link active state (per UI-SPEC interaction contract):

```css
[data-nav-link][data-active="true"] {
  color: var(--color-accent-primary) !important;
}
[data-nav-link][data-active="true"].nav-link {
  text-decoration: underline;
  text-underline-offset: 4px;
  text-decoration-thickness: 2px;
}
[data-nav-link]:hover {
  color: var(--color-text) !important;
}
```

The full file should preserve the existing frontmatter (imports of @fontsource-variable/inter, global.css, Head, ThemeToggle), Props interface, and body/main structure.
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && grep -q 'data-nav-link' src/layouts/BaseLayout.astro && grep -q 'hamburger-btn' src/layouts/BaseLayout.astro && grep -q 'IntersectionObserver' src/layouts/BaseLayout.astro && grep -q 'scrollIntoView' src/layouts/BaseLayout.astro && grep -q 'aria-expanded' src/layouts/BaseLayout.astro && grep -q 'aria-label="Main navigation"' src/layouts/BaseLayout.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/BaseLayout.astro contains `aria-label="Main navigation"` on nav element
    - src/layouts/BaseLayout.astro contains `href="#about"` with `data-nav-link`
    - src/layouts/BaseLayout.astro contains `href="#experience"` with `data-nav-link`
    - src/layouts/BaseLayout.astro contains `href="#projects"` with `data-nav-link`
    - src/layouts/BaseLayout.astro contains `href="#contact"` with `data-nav-link`
    - src/layouts/BaseLayout.astro contains `id="hamburger-btn"` with `aria-label="Open navigation menu"`
    - src/layouts/BaseLayout.astro contains `aria-expanded="false"` on hamburger button
    - src/layouts/BaseLayout.astro contains `id="mobile-menu"` with `class="hidden md:hidden`
    - src/layouts/BaseLayout.astro contains `new IntersectionObserver`
    - src/layouts/BaseLayout.astro contains `rootMargin: '-40% 0px -55% 0px'`
    - src/layouts/BaseLayout.astro contains `scrollIntoView({ behavior: 'smooth' })`
    - src/layouts/BaseLayout.astro does NOT contain `scroll-behavior: smooth` (no global CSS smooth scroll)
    - src/layouts/BaseLayout.astro still contains `<ThemeToggle />`
    - src/layouts/BaseLayout.astro still contains `<Head title={title}`
  </acceptance_criteria>
  <done>Nav bar shows About/Experience/Projects/Contact links on desktop, hamburger menu on mobile with ThemeToggle always visible, Intersection Observer highlights active section, and smooth scroll works on nav link clicks.</done>
</task>

<task type="auto">
  <name>Task 2: Create static analysis test file for all Phase 4 requirements</name>
  <files>tests/projects-contact.test.ts</files>
  <read_first>
    - tests/cv-section.test.ts (established test pattern — static file analysis with readFileSync)
    - src/data/projects.ts (verify structure assertions)
    - src/data/contact.ts (verify structure assertions)
    - src/components/ProjectsSection.astro (verify content assertions)
    - src/components/ContactSection.astro (verify content assertions)
    - src/layouts/BaseLayout.astro (verify nav assertions)
    - src/pages/index.astro (verify integration assertions)
  </read_first>
  <action>
Create `tests/projects-contact.test.ts` following the exact pattern from `tests/cv-section.test.ts` — static file analysis using `readFileSync` and `existsSync`.

```typescript
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { join } from "node:path";

const ROOT = join(import.meta.dirname, "..");

const readFile = (rel: string) =>
  readFileSync(join(ROOT, rel), "utf-8");

// PROJ-01: mytai card with description, tech stack summary, and link to mytai.uk
describe("PROJ-01: mytai project card", () => {
  const projectCard = readFile("src/components/ProjectCard.astro");
  const projectsData = readFile("src/data/projects.ts");

  it("ProjectCard.astro exists", () => {
    expect(existsSync(join(ROOT, "src/components/ProjectCard.astro"))).toBe(true);
  });

  it("projects.ts contains mytai title", () => {
    expect(projectsData).toContain('title: "mytai"');
  });

  it("projects.ts contains mytai.uk URL", () => {
    expect(projectsData).toContain("https://mytai.uk");
  });

  it("projects.ts exports typed Project interface", () => {
    expect(projectsData).toContain("export interface Project");
  });

  it("projects.ts uses immutable as const satisfies pattern", () => {
    expect(projectsData).toContain("as const satisfies");
  });

  it("ProjectCard contains device frame placeholder", () => {
    expect(projectCard).toContain('aria-label="mytai app preview"');
  });

  it("ProjectCard contains dual-accent gradient", () => {
    expect(projectCard).toContain("rgba(99,102,241,0.4)");
  });

  it("ProjectCard contains tech stack badge styling matching SkillsGrid", () => {
    expect(projectCard).toContain("rounded-full text-sm font-medium");
  });

  it("ProjectCard contains CTA link with target blank and rel", () => {
    expect(projectCard).toContain('target="_blank"');
    expect(projectCard).toContain('rel="noopener noreferrer"');
  });

  it("ProjectCard CTA uses accent-primary background", () => {
    expect(projectCard).toContain("var(--color-accent-primary)");
  });
});

// PROJ-02: Structure allows adding more project cards
describe("PROJ-02: Scalable projects structure", () => {
  const projectsSection = readFile("src/components/ProjectsSection.astro");
  const projectsData = readFile("src/data/projects.ts");

  it("ProjectsSection.astro exists", () => {
    expect(existsSync(join(ROOT, "src/components/ProjectsSection.astro"))).toBe(true);
  });

  it("ProjectsSection uses grid layout", () => {
    expect(projectsSection).toContain("grid grid-cols-1");
  });

  it("ProjectsSection maps over projects array", () => {
    expect(projectsSection).toMatch(/projects\.map/);
  });

  it("projects.ts exports a readonly array", () => {
    expect(projectsData).toContain("readonly Project[]");
  });

  it("ProjectsSection has id='projects'", () => {
    expect(projectsSection).toContain('id="projects"');
  });
});

// CONT-01: Contact section with email, LinkedIn, and GitHub links
describe("CONT-01: Contact section", () => {
  const contactSection = readFile("src/components/ContactSection.astro");
  const contactData = readFile("src/data/contact.ts");

  it("ContactSection.astro exists", () => {
    expect(existsSync(join(ROOT, "src/components/ContactSection.astro"))).toBe(true);
  });

  it("contact.ts contains email iconType", () => {
    expect(contactData).toContain('iconType: "email"');
  });

  it("contact.ts contains linkedin iconType", () => {
    expect(contactData).toContain('iconType: "linkedin"');
  });

  it("contact.ts contains github iconType", () => {
    expect(contactData).toContain('iconType: "github"');
  });

  it("contact.ts contains mailto: scheme", () => {
    expect(contactData).toContain("mailto:");
  });

  it("contact.ts uses immutable as const satisfies pattern", () => {
    expect(contactData).toContain("as const satisfies");
  });

  it("ContactSection has id='contact'", () => {
    expect(contactSection).toContain('id="contact"');
  });

  it("ContactSection contains Get in Touch heading", () => {
    expect(contactSection).toContain("Get in Touch");
  });

  it("ContactSection external links have rel noopener noreferrer", () => {
    expect(contactSection).toContain("noopener noreferrer");
  });

  it("ContactSection contains aria-hidden SVG icons", () => {
    expect(contactSection).toContain('aria-hidden="true"');
  });
});

// CONT-02: Contact reachable from any section (persistent nav)
describe("CONT-02: Persistent navigation", () => {
  const layout = readFile("src/layouts/BaseLayout.astro");
  const indexPage = readFile("src/pages/index.astro");

  it("BaseLayout nav has aria-label Main navigation", () => {
    expect(layout).toContain('aria-label="Main navigation"');
  });

  it("Nav contains About anchor link", () => {
    expect(layout).toContain('href="#about"');
  });

  it("Nav contains Experience anchor link", () => {
    expect(layout).toContain('href="#experience"');
  });

  it("Nav contains Projects anchor link", () => {
    expect(layout).toContain('href="#projects"');
  });

  it("Nav contains Contact anchor link", () => {
    expect(layout).toContain('href="#contact"');
  });

  it("Nav has hamburger button with aria-label", () => {
    expect(layout).toContain('aria-label="Open navigation menu"');
  });

  it("Nav has hamburger button with aria-expanded", () => {
    expect(layout).toContain('aria-expanded="false"');
  });

  it("Nav uses Intersection Observer for active state", () => {
    expect(layout).toContain("IntersectionObserver");
  });

  it("Nav uses smooth scrollIntoView (not CSS scroll-behavior)", () => {
    expect(layout).toContain("scrollIntoView");
    expect(layout).not.toContain("scroll-behavior: smooth");
  });

  it("index.astro renders ProjectsSection", () => {
    expect(indexPage).toContain("ProjectsSection");
  });

  it("index.astro renders ContactSection", () => {
    expect(indexPage).toContain("ContactSection");
  });

  it("AboutSection has id='about' for nav anchor", () => {
    const about = readFile("src/components/AboutSection.astro");
    expect(about).toContain('id="about"');
  });
});
```

This file covers all 4 requirements (PROJ-01, PROJ-02, CONT-01, CONT-02) with ~35 test assertions using the established static file analysis pattern.
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/projects-contact.test.ts exists
    - tests/projects-contact.test.ts contains `describe("PROJ-01`
    - tests/projects-contact.test.ts contains `describe("PROJ-02`
    - tests/projects-contact.test.ts contains `describe("CONT-01`
    - tests/projects-contact.test.ts contains `describe("CONT-02`
    - `npx vitest run` exits with code 0 (all tests pass)
    - Total test count is 162 + ~35 = ~197 tests
  </acceptance_criteria>
  <done>All Phase 4 requirements have automated test coverage. Full test suite (existing 162 + new ~35) passes green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of projects card, contact section, and navigation</name>
  <files>none</files>
  <action>
Human visual verification of all Phase 4 work. Run `npx astro dev` and open http://localhost:4321 to verify:

1. **Desktop nav (D-06, D-08, D-09):** About, Experience, Projects, Contact links visible in nav bar. Click each — smooth-scrolls to correct section. Scroll through page — active link highlights as sections enter viewport.
2. **Mobile nav (D-07):** Resize to ~375px. Hamburger icon appears, section links hidden. Click hamburger — dropdown with all 4 links. Click a link — scrolls and closes menu. ThemeToggle stays visible.
3. **Projects section (D-01, D-02, D-03, D-04):** mytai card shows device frame placeholder with gradient on left (top on mobile), project title, tagline, description, feature bullets, tech stack badges, and "Visit mytai.uk" button opening https://mytai.uk in new tab.
4. **Contact section (D-10, D-11, D-12, D-13):** Center-aligned with "Get in Touch" heading, message, three icon links (Email, LinkedIn, GitHub). Hover changes border/text to accent color. Email opens mail client. LinkedIn/GitHub open in new tabs.
5. **Dark/light mode:** Toggle theme — all new components transition smoothly.
6. **Hero CTA:** "View my work" button scrolls to Projects section.
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>All Phase 4 visual and interactive elements confirmed working by user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav -> same-page anchors | All nav hrefs are #anchor format — no cross-origin navigation |
| contact -> external URLs | LinkedIn/GitHub links open in new tabs |
| contact -> mailto handler | Email link triggers OS mail client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | External links in ContactSection and ProjectCard (target="_blank") | mitigate | `rel="noopener noreferrer"` on all external links per D-12 |
| T-04-02 | Information Disclosure | Email address in mailto link | accept | Email is intentionally public on portfolio site |
| T-04-03 | Tampering | Nav anchor hrefs | accept | All hrefs are same-page `#anchor` — no user-controlled redirect |
| T-04-04 | Denial of Service | Intersection Observer on many sections | accept | Only 4 sections observed; native browser API with negligible overhead |
</threat_model>

<verification>
- `npx vitest run` passes all tests (existing 162 + new ~35)
- `npx astro check` completes with no errors
- Nav links scroll to correct sections
- Hamburger menu works on mobile
- Active section highlighting updates on scroll
- All external links open in new tabs with noopener noreferrer
</verification>

<success_criteria>
- Nav bar shows About, Experience, Projects, Contact links on desktop
- Hamburger menu collapses links on mobile with ThemeToggle visible
- Active section highlighted in nav during scroll
- Smooth scroll on nav link clicks
- All ~197 tests pass green
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/04-projects-contact/04-02-SUMMARY.md`
</output>
