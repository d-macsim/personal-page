---
phase: 03-cv-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/SkillsGrid.astro
  - src/components/CVDownloadButton.astro
  - src/components/CVSection.astro
  - tests/cv-section.test.ts
autonomous: true
requirements:
  - EXP-03
  - EXP-04

must_haves:
  truths:
    - "Skills are displayed as categorized badge/chips grouped by category name"
    - "No percentage bars, progress elements, or meter elements exist anywhere on the page"
    - "A prominent download button at the bottom of the CV section downloads Dragos Macsim CV 2026.pdf"
    - "Download link has correct href and download attribute with exact filename"
    - "All tests pass verifying EXP-01 through EXP-04"
  artifacts:
    - path: "src/components/SkillsGrid.astro"
      provides: "Categorized skill badge display"
      contains: "color-surface"
    - path: "src/components/CVDownloadButton.astro"
      provides: "PDF download CTA button"
      contains: "Dragos Macsim CV 2026.pdf"
    - path: "src/components/CVSection.astro"
      provides: "Complete section with timeline, skills, and download button"
      contains: "SkillsGrid"
    - path: "tests/cv-section.test.ts"
      provides: "Static file analysis tests for EXP-01 through EXP-04"
      contains: "EXP-01"
  key_links:
    - from: "src/components/SkillsGrid.astro"
      to: "src/data/cv.ts"
      via: "Props typed with SkillCategory[]"
      pattern: "SkillCategory"
    - from: "src/components/CVDownloadButton.astro"
      to: "public/Dragos Macsim CV 2026.pdf"
      via: "href attribute pointing to /Dragos Macsim CV 2026.pdf"
      pattern: "Dragos Macsim CV 2026.pdf"
    - from: "src/components/CVSection.astro"
      to: "src/components/SkillsGrid.astro"
      via: "Astro import and render"
      pattern: "import SkillsGrid"
---

<objective>
Complete the CV section with skills grid, download button, and verification tests.

Purpose: Delivers EXP-03 (categorized skill badges, no percentage bars) and EXP-04 (downloadable CV PDF), plus static analysis tests covering all four phase requirements.
Output: `SkillsGrid.astro`, `CVDownloadButton.astro`, completed `CVSection.astro`, `tests/cv-section.test.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cv-experience/03-CONTEXT.md
@.planning/phases/03-cv-experience/03-RESEARCH.md
@.planning/phases/03-cv-experience/03-UI-SPEC.md
@.planning/phases/03-cv-experience/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From src/data/cv.ts (created in Plan 01):
```typescript
export interface SkillCategory {
  category: string;
  skills: string[];
}

export const skillCategories: readonly SkillCategory[] = [
  { category: "Languages & Data", skills: ["Python", "R", "SQL", "Excel"] },
  { category: "AI & ML", skills: ["RAG", "LLM Evaluation", "Deep Learning", "BeautifulSoup", "Selenium"] },
  { category: "Tools & Infrastructure", skills: ["Tableau", "Docker", "Ubuntu Server", "Git"] },
];
```

From src/components/CVSection.astro (shell from Plan 01):
```astro
---
import ExperienceTimeline from "./ExperienceTimeline.astro";
import { roles, education } from "../data/cv";
---
<section id="experience" aria-label="Experience and CV" class="py-16 md:py-24">
  <h2 class="font-semibold mb-12" style={{ fontSize: "var(--font-size-heading)" }}>Experience</h2>
  <div class="max-w-2xl">
    <ExperienceTimeline roles={roles} education={education} />
  </div>
</section>
```

From src/styles/global.css — tokens used:
```css
--color-surface: #111118;
--color-border: #27272a;
--color-text: #e4e4e7;
--color-text-muted: #71717a;
--color-accent-primary: #6366f1;
--color-accent-primary-hover: #818cf8;
--color-accent-primary-press: #4f46e5;
--font-size-label: 0.875rem;
--font-size-heading: clamp(1.25rem, 3vw, 2.5rem);
```

From tests/about-section.test.ts — test pattern:
```typescript
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { join } from "node:path";
const ROOT = join(import.meta.dirname, "..");
const readFile = (rel: string) => readFileSync(join(ROOT, rel), "utf-8");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkillsGrid, CVDownloadButton, and complete CVSection</name>
  <files>src/components/SkillsGrid.astro, src/components/CVDownloadButton.astro, src/components/CVSection.astro</files>
  <read_first>
    src/data/cv.ts
    src/components/CVSection.astro
    src/components/HighlightCard.astro
    src/styles/global.css
    .planning/phases/03-cv-experience/03-UI-SPEC.md
  </read_first>
  <action>
**SkillsGrid.astro** — Create categorized skill badge display per D-06, D-07:

```astro
---
import type { SkillCategory } from "../data/cv";

interface Props {
  categories: readonly SkillCategory[];
}

const { categories } = Astro.props;
---
```

For each category in `categories`:
- Category wrapper: `<div class="mb-6">`
- Category heading: `<h3 class="text-xs font-semibold uppercase tracking-widest mb-3" style="font-size: var(--font-size-label); color: var(--color-text-muted);">{cat.category}</h3>`
- Badge row: `<div class="flex flex-wrap gap-2">`
- Each skill badge: `<span class="px-3 py-1 rounded-full text-sm font-medium transition-colors duration-200" style="background-color: var(--color-surface); border: 1px solid var(--color-border); color: var(--color-text);">{skill}</span>`

CRITICAL per D-07: No `<progress>`, `<meter>`, percentage-width `<div>`, or any visual element implying proficiency level. Badges are display-only with no hover state.

**CVDownloadButton.astro** — Create download CTA per D-08, D-09:

```astro
<a
  href="/Dragos Macsim CV 2026.pdf"
  download="Dragos Macsim CV 2026.pdf"
  aria-label="Download Dragos Macsim CV as PDF"
  class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-semibold transition-colors duration-200"
  style="background-color: var(--color-accent-primary); color: white;"
>
```

Content inside the anchor:
- Inline SVG download arrow icon (16x16, `currentColor` fill, `aria-hidden="true"`): a simple downward arrow with a horizontal line at the bottom
- Text: `Download CV`

Add hover/press states via inline style or a `<style>` block scoped to the component:
- Hover: `background-color: var(--color-accent-primary-hover)`
- Active: `background-color: var(--color-accent-primary-press)`

Use a scoped `<style>` block in the Astro component for the hover/active states since inline styles cannot handle pseudo-classes:
```css
a:hover { background-color: var(--color-accent-primary-hover) !important; }
a:active { background-color: var(--color-accent-primary-press) !important; }
```

Button must be solid indigo primary (per D-09 — solid chosen over ghost because this is the primary conversion CTA at section end).

**CVSection.astro** — Update the shell from Plan 01 to include SkillsGrid and CVDownloadButton:

Add imports:
```astro
import SkillsGrid from "./SkillsGrid.astro";
import CVDownloadButton from "./CVDownloadButton.astro";
import { roles, education, skillCategories } from "../data/cv";
```

Add after the ExperienceTimeline `</div>`:
```astro
<div class="max-w-2xl mt-16">
  <h2
    class="font-semibold mb-8"
    style={{ fontSize: "var(--font-size-heading)" }}
  >
    Skills
  </h2>
  <SkillsGrid categories={skillCategories} />
</div>

<div class="mt-12">
  <CVDownloadButton />
</div>
```

The final CVSection.astro must have:
- `id="experience"` and `aria-label="Experience and CV"` on the outer `<section>`
- `py-16 md:py-24` section padding
- ExperienceTimeline with roles and education props
- Skills h2 heading
- SkillsGrid with categories prop
- CVDownloadButton at the bottom with `mt-12`
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SkillsGrid.astro exists
    - SkillsGrid.astro contains `import type { SkillCategory } from "../data/cv"`
    - SkillsGrid.astro contains `background-color: var(--color-surface)` (badge background per D-06)
    - SkillsGrid.astro contains `border: 1px solid var(--color-border)` (badge border per D-06)
    - SkillsGrid.astro contains `rounded-full` (pill-shaped badges)
    - SkillsGrid.astro does NOT contain `progress` or `meter` or `width:` with percentage (per D-07)
    - src/components/CVDownloadButton.astro exists
    - CVDownloadButton.astro contains `href="/Dragos Macsim CV 2026.pdf"`
    - CVDownloadButton.astro contains `download="Dragos Macsim CV 2026.pdf"`
    - CVDownloadButton.astro contains `aria-label="Download Dragos Macsim CV as PDF"`
    - CVDownloadButton.astro contains `background-color: var(--color-accent-primary)` (solid indigo per D-09)
    - CVDownloadButton.astro contains `Download CV` text
    - CVDownloadButton.astro contains `<svg` (download arrow icon)
    - src/components/CVSection.astro contains `import SkillsGrid`
    - src/components/CVSection.astro contains `import CVDownloadButton`
    - src/components/CVSection.astro contains `skillCategories` import from data/cv
    - CVSection.astro contains `<SkillsGrid` and `<CVDownloadButton`
    - CVSection.astro contains an h2 with text `Skills`
    - `npx astro build` completes without errors
  </acceptance_criteria>
  <done>SkillsGrid renders 3 skill categories as badge/chips with no percentage bars (EXP-03). CVDownloadButton links to the correct PDF with download attribute (EXP-04). CVSection is complete with timeline, skills, and download button. Build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create static analysis tests for EXP-01 through EXP-04</name>
  <files>tests/cv-section.test.ts</files>
  <read_first>
    tests/about-section.test.ts
    src/components/CVSection.astro
    src/components/ExperienceTimeline.astro
    src/components/SkillsGrid.astro
    src/components/CVDownloadButton.astro
    src/data/cv.ts
  </read_first>
  <behavior>
    - EXP-01: ExperienceTimeline contains all 3 role titles ("AI Specialist", "Data Analyst", "Guest Relations Manager"), all 3 companies ("Mindrift", "Scale AI", "London House Hotel"), date ranges, and achievement bullet iteration
    - EXP-02: ExperienceTimeline contains "Education" separator label, education entries rendered with degree/institution/detail/modules pattern
    - EXP-03: SkillsGrid contains no `progress`, `meter`, or percentage-width elements; contains badge styling (`rounded-full`, `color-surface`)
    - EXP-04: CVDownloadButton contains `href="/Dragos Macsim CV 2026.pdf"`, `download="Dragos Macsim CV 2026.pdf"`, and `aria-label`
    - Integration: CVSection imports ExperienceTimeline, SkillsGrid, and CVDownloadButton; has `id="experience"`; index.astro imports and renders CVSection
    - Data file: cv.ts exports roles (3 items), education (2 items), skillCategories (3 items) with correct content
  </behavior>
  <action>
Create `tests/cv-section.test.ts` following the exact same pattern as `tests/about-section.test.ts` — using vitest with `readFileSync` for static file analysis.

```typescript
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { join } from "node:path";

const ROOT = join(import.meta.dirname, "..");

const readFile = (rel: string) =>
  readFileSync(join(ROOT, rel), "utf-8");
```

**Test groups:**

`describe("EXP-01: Experience timeline")` — read `src/components/ExperienceTimeline.astro` and `src/data/cv.ts`:
- ExperienceTimeline.astro exists
- cv.ts contains "AI Specialist" (role title)
- cv.ts contains "Scale AI" (company)
- cv.ts contains "London House Hotel" (company)
- cv.ts contains `achievements` property (each role has achievements array)
- roles array has 3 entries: count occurrences of `title:` in the roles section
- ExperienceTimeline.astro contains `role.achievements` or `.achievements.map` (iterates achievements)
- ExperienceTimeline.astro contains `color-accent-primary` (dot markers per D-02)
- ExperienceTimeline.astro contains `color-border` (spine line per D-02)

`describe("EXP-02: Education section")` — read same files:
- cv.ts contains "MSc Business Analytics"
- cv.ts contains "University College London"
- cv.ts contains "On track for distinction"
- cv.ts contains "Upper second class honours"
- cv.ts contains `modules` property
- ExperienceTimeline.astro contains text "Education" (separator label per D-04)
- ExperienceTimeline.astro contains `entry.modules` (conditional rendering per D-05)

`describe("EXP-03: Skills display")` — read `src/components/SkillsGrid.astro` and `src/data/cv.ts`:
- SkillsGrid.astro exists
- cv.ts contains "Languages & Data" (category name)
- cv.ts contains "AI & ML" (category name)
- cv.ts contains "Tools & Infrastructure" (category name)
- cv.ts contains "Python" (skill)
- SkillsGrid.astro contains `rounded-full` (badge shape)
- SkillsGrid.astro does NOT contain `<progress` (no percentage bars)
- SkillsGrid.astro does NOT contain `<meter` (no percentage bars)
- SkillsGrid.astro contains `color-surface` (badge background token)

`describe("EXP-04: CV download")` — read `src/components/CVDownloadButton.astro`:
- CVDownloadButton.astro exists
- Contains `href="/Dragos Macsim CV 2026.pdf"`
- Contains `download="Dragos Macsim CV 2026.pdf"`
- Contains `aria-label` attribute
- Contains `color-accent-primary` (solid button style)
- Public PDF exists: `existsSync(join(ROOT, "public/Dragos Macsim CV 2026.pdf"))` is true

`describe("CV section integration")` — read `src/components/CVSection.astro` and `src/pages/index.astro`:
- CVSection.astro contains `id="experience"`
- CVSection.astro contains `aria-label="Experience and CV"`
- CVSection.astro contains `import ExperienceTimeline`
- CVSection.astro contains `import SkillsGrid`
- CVSection.astro contains `import CVDownloadButton`
- index.astro contains `import CVSection`
- index.astro contains `<CVSection`
- index.astro does NOT match `/CVSection\s+client:/` (no React island directive)
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npx vitest run tests/cv-section.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/cv-section.test.ts exists
    - File contains `describe("EXP-01` test group
    - File contains `describe("EXP-02` test group
    - File contains `describe("EXP-03` test group
    - File contains `describe("EXP-04` test group
    - File contains `describe("CV section integration"` test group
    - File contains check for `<progress` not present (negative assertion for EXP-03)
    - File contains check for `download="Dragos Macsim CV 2026.pdf"` (EXP-04)
    - `npx vitest run tests/cv-section.test.ts` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>All static analysis tests pass covering EXP-01 (3 roles with achievements), EXP-02 (2 education entries with details), EXP-03 (skill badges, no % bars), EXP-04 (PDF download with correct filename). Test file follows established vitest + readFileSync pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 3 is static content with no user input, no API calls, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | PDF download link | accept | PDF is intentionally public — it is the user's CV meant for download by visitors. No sensitive data beyond what is already visible on-page. |
| T-03-02 | Tampering | Static content | accept | Content is build-time static HTML served from Cloudflare Pages CDN. No runtime data sources to tamper with. |
</threat_model>

<verification>
1. `npx astro build` completes with zero errors
2. `npx vitest run tests/cv-section.test.ts` — all tests pass
3. Built HTML contains skill badges with `rounded-full` class (no percentage bars)
4. Built HTML contains `download="Dragos Macsim CV 2026.pdf"` on an anchor element
5. No `<progress>` or `<meter>` elements in any component file
</verification>

<success_criteria>
- Skills displayed as categorized badge/chips with no percentage bars anywhere (EXP-03)
- Download button links to correct PDF with `download` attribute and exact filename (EXP-04)
- All static analysis tests pass for EXP-01 through EXP-04
- Astro build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-cv-experience/03-02-SUMMARY.md`
</output>
