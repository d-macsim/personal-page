---
phase: 05-animations-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/AboutSection.astro
  - src/components/CVSection.astro
  - src/components/ExperienceTimeline.astro
  - src/components/SkillsGrid.astro
  - src/components/ProjectsSection.astro
  - src/components/ContactSection.astro
  - tests/phase5-animations.test.ts
autonomous: true
requirements:
  - DSGN-04

must_haves:
  truths:
    - "Each non-hero section reveals with a fade-up animation as the user scrolls it into view"
    - "Animations are CSS-only — no JavaScript is added for scroll reveals"
    - "Browsers without animation-timeline support show all content immediately"
    - "Users with prefers-reduced-motion see all content immediately with no animation"
    - "Experience timeline items and skill category blocks stagger their entrance"
  artifacts:
    - path: "src/styles/global.css"
      provides: "@keyframes reveal-up, .reveal class, .reveal-stagger rules, @supports guards, reduced-motion override"
      contains: "@keyframes reveal-up"
    - path: "src/components/AboutSection.astro"
      provides: "reveal class on content wrapper"
      contains: "reveal"
    - path: "src/components/ExperienceTimeline.astro"
      provides: "reveal-stagger class on timeline entries"
      contains: "reveal-stagger"
    - path: "src/components/SkillsGrid.astro"
      provides: "reveal-stagger class on category blocks"
      contains: "reveal-stagger"
    - path: "src/components/ProjectsSection.astro"
      provides: "reveal class on section inner container"
      contains: "reveal"
    - path: "src/components/ContactSection.astro"
      provides: "reveal class on content wrapper"
      contains: "reveal"
    - path: "tests/phase5-animations.test.ts"
      provides: "Static analysis tests for animation CSS and component classes"
  key_links:
    - from: "src/components/*.astro"
      to: "src/styles/global.css"
      via: ".reveal and .reveal-stagger CSS classes"
      pattern: "class=.*reveal"
---

<objective>
Add CSS scroll-driven reveal animations to all non-hero sections using animation-timeline: view().

Purpose: Sections currently appear statically. Adding subtle fade-up entrance animations as the user scrolls makes section transitions feel polished and professional, matching the hero's Motion v12 entrance but implemented with zero JavaScript.

Output: Updated global.css with @keyframes, .reveal, .reveal-stagger rules; all six non-hero section components updated with reveal classes; static analysis tests verifying the implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-animations-deploy/05-CONTEXT.md
@.planning/phases/05-animations-deploy/05-RESEARCH.md
@.planning/phases/05-animations-deploy/05-UI-SPEC.md

<interfaces>
<!-- Existing global.css structure the executor needs to extend -->
From src/styles/global.css:
- Existing @keyframes glowPulse block at lines 115-118
- Existing .glow-pulse class at lines 120-123
- Existing @media (prefers-reduced-motion: reduce) block at lines 125-129 covering .glow-pulse only
- File ends at line 130

From src/components/ExperienceTimeline.astro:
- Timeline entries rendered via roles.map() at line 21, each wrapped in <div class="relative pl-0 sm:pl-12 pb-8 sm:pb-12">
- Education entries rendered via education.map() at line 73, same wrapper structure
- Root element is <div class="relative"> at line 12

From src/components/SkillsGrid.astro:
- Categories rendered via categories.map() at line 12, each in <div class="mb-6">
- Root is a fragment (no wrapper div)

From src/components/AboutSection.astro:
- Root is <section id="about"> at line 6
- Inner content: prose div at line 19, highlight cards grid at line 42

From src/components/CVSection.astro:
- Root is <section id="experience"> at line 8
- Contains ExperienceTimeline, SkillsGrid, CVDownloadButton as children

From src/components/ProjectsSection.astro:
- Root is <section id="projects"> at line 6
- Inner container: <div class="max-w-[1100px] mx-auto"> at line 7

From src/components/ContactSection.astro:
- Root is <section id="contact"> at line 5
- Inner container: <div class="max-w-[1100px] mx-auto text-center"> at line 6
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scroll-reveal CSS to global.css and write tests</name>
  <files>src/styles/global.css, tests/phase5-animations.test.ts</files>
  <read_first>
    - src/styles/global.css (current state — extend, do not replace existing rules)
    - .planning/phases/05-animations-deploy/05-UI-SPEC.md (animation contracts — exact keyframe, class, and range values)
    - .planning/phases/05-animations-deploy/05-RESEARCH.md (patterns and anti-patterns)
    - tests/design-system.test.ts (existing test pattern — static file analysis with readFileSync)
  </read_first>
  <behavior>
    - Test 1: global.css contains `@keyframes reveal-up` with `opacity: 0` and `translateY(16px)` in the `from` block
    - Test 2: global.css contains `.reveal` class with `animation-timeline: view()` and `animation-range: entry`
    - Test 3: global.css contains `.reveal-stagger` class with `animation-timeline: view()`
    - Test 4: global.css contains `@supports not (animation-timeline: view())` fallback that sets `opacity: 1` and `animation: none`
    - Test 5: global.css contains `prefers-reduced-motion: reduce` rule covering both `.reveal` and `.reveal-stagger`
    - Test 6: global.css does NOT contain `.reveal` applied to `#hero` or `HeroSection`
    - Test 7: AboutSection.astro contains class attribute with `reveal`
    - Test 8: ExperienceTimeline.astro contains class attribute with `reveal-stagger`
    - Test 9: SkillsGrid.astro contains class attribute with `reveal-stagger`
    - Test 10: ProjectsSection.astro contains class attribute with `reveal`
    - Test 11: ContactSection.astro contains class attribute with `reveal`
    - Test 12: CVSection.astro contains class attribute with `reveal`
  </behavior>
  <action>
    Write tests first (RED), then add the following CSS block to the END of src/styles/global.css (after the existing prefers-reduced-motion block at line 130):

    1. Add a comment separator: `/* ---- Scroll-Reveal Animation (Phase 5) ---- */`

    2. Add @keyframes outside @supports (per UI-SPEC):
    ```css
    @keyframes reveal-up {
      from {
        opacity: 0;
        transform: translateY(16px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    ```

    3. Add @supports (animation-timeline: view()) block containing:
    - `.reveal` class: `animation: reveal-up 0.5s ease-out both; animation-timeline: view(); animation-range: entry 0% entry 40%;`
    - `.reveal-stagger > *` class: `animation: reveal-up 0.5s ease-out both; animation-timeline: view(); animation-range: entry 0% entry 50%;`
    - `.reveal-stagger > *:nth-child(N)` rules for N=1 through 6 with animation-delay: 0ms, 80ms, 160ms, 240ms, 320ms, 400ms
    - `.reveal-stagger > *:nth-child(n+7)` with animation-delay: 400ms (cap)
    - `@media (prefers-reduced-motion: reduce)` nested inside @supports: `.reveal, .reveal-stagger > * { animation: none; opacity: 1; transform: none; }`

    4. Add @supports NOT block (outside the positive @supports):
    ```css
    @supports not (animation-timeline: view()) {
      .reveal,
      .reveal-stagger > * {
        opacity: 1;
        transform: none;
        animation: none;
      }
    }
    ```

    IMPORTANT: Do NOT modify the existing .glow-pulse or prefers-reduced-motion block for .glow-pulse. Add new rules AFTER them.
    IMPORTANT: Do NOT add any JavaScript for scroll detection. Per D-04, this is CSS-only.
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - global.css contains `@keyframes reveal-up` with `translateY(16px)` in `from` block
    - global.css contains `@supports (animation-timeline: view())` block
    - global.css contains `.reveal` with `animation-timeline: view()`
    - global.css contains `.reveal-stagger > *` with `animation-timeline: view()`
    - global.css contains `@supports not (animation-timeline: view())` fallback block with `opacity: 1` and `animation: none`
    - global.css contains `prefers-reduced-motion: reduce` inside @supports covering `.reveal` and `.reveal-stagger`
    - tests/phase5-animations.test.ts exists and all tests pass with `npm test`
    - No changes to HeroSection.tsx
    - No new npm packages added
  </acceptance_criteria>
  <done>global.css has complete scroll-reveal CSS system with @keyframes, @supports guards, stagger rules, reduced-motion override, and browser fallback. Tests verify all CSS rules exist.</done>
</task>

<task type="auto">
  <name>Task 2: Apply reveal classes to all non-hero section components</name>
  <files>src/components/AboutSection.astro, src/components/CVSection.astro, src/components/ExperienceTimeline.astro, src/components/SkillsGrid.astro, src/components/ProjectsSection.astro, src/components/ContactSection.astro</files>
  <read_first>
    - src/components/AboutSection.astro (current markup — identify content wrapper for .reveal)
    - src/components/CVSection.astro (current markup — identify wrapper)
    - src/components/ExperienceTimeline.astro (current markup — identify each timeline entry div for .reveal-stagger)
    - src/components/SkillsGrid.astro (current markup — identify category blocks for .reveal-stagger)
    - src/components/ProjectsSection.astro (current markup — identify inner container)
    - src/components/ContactSection.astro (current markup — identify content wrapper)
    - .planning/phases/05-animations-deploy/05-UI-SPEC.md (Per-Section Application table — which sections get .reveal vs .reveal-stagger)
  </read_first>
  <action>
    Apply reveal classes per the UI-SPEC Per-Section Application table. Exact changes for each file:

    **AboutSection.astro** — Single reveal on inner content. The section has three content blocks (heading, photo, prose, highlight cards). Wrap the content BELOW the heading in a container div with class="reveal":
    - Do NOT wrap the `<section>` itself (anti-pattern per RESEARCH.md)
    - Add `class="reveal"` to a new wrapper `<div>` around lines 14-49 (photo + prose + highlight cards)

    **CVSection.astro** — The section contains ExperienceTimeline, SkillsGrid, and CVDownloadButton. Apply `.reveal` to the `<section>` inner content — but since children (ExperienceTimeline, SkillsGrid) handle their own stagger, apply `.reveal` to:
    - The heading `<h2>` element (wrap in a `<div class="reveal">`)
    - The Skills heading `<h2>` (wrap in a `<div class="reveal">`)
    - The CVDownloadButton wrapper `<div class="mt-12">` — add `reveal` to its class list: `class="mt-12 reveal"`

    **ExperienceTimeline.astro** — Staggered children. The root `<div class="relative">` at line 12 serves as the container for timeline entries. Add `reveal-stagger` to the root div's class: `class="relative reveal-stagger"`. Each child `<div class="relative pl-0 sm:pl-12 pb-8 sm:pb-12">` is a direct child and will be targeted by `.reveal-stagger > *`.

    **SkillsGrid.astro** — Staggered children. The component renders category divs as siblings (no wrapper). Wrap all output in a `<div class="reveal-stagger">` parent so that each `<div class="mb-6">` category block becomes a direct child targeted by `.reveal-stagger > *`.

    **ProjectsSection.astro** — Single reveal. Add `reveal` to the inner container div: change `class="max-w-[1100px] mx-auto"` to `class="max-w-[1100px] mx-auto reveal"`.

    **ContactSection.astro** — Single reveal. Add `reveal` to the inner container div: change `class="max-w-[1100px] mx-auto text-center"` to `class="max-w-[1100px] mx-auto text-center reveal"`.

    IMPORTANT: Do NOT touch HeroSection.tsx — it already has Motion v12 animations.
    IMPORTANT: Do NOT add any JavaScript or React imports for animation purposes.
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npm test -- --reporter=verbose 2>&1 | tail -30 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AboutSection.astro contains a div with class including `reveal`
    - CVSection.astro contains elements with class including `reveal`
    - ExperienceTimeline.astro root div class contains `reveal-stagger`
    - SkillsGrid.astro has a wrapper div with class `reveal-stagger`
    - ProjectsSection.astro inner container class contains `reveal`
    - ContactSection.astro inner container class contains `reveal`
    - HeroSection.tsx is UNCHANGED (no reveal class, no modifications)
    - `npm run build` exits with code 0 (no compilation errors)
    - All tests in tests/phase5-animations.test.ts pass
  </acceptance_criteria>
  <done>All six non-hero sections have appropriate reveal/reveal-stagger classes applied. Build succeeds. No JavaScript added. Hero untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 5 Plan 01 adds CSS-only rules — no new data flows, user inputs, or external integrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | CSS animation on low-powered devices | accept | CSS scroll-driven animations run off main thread in supported browsers. prefers-reduced-motion disables all animation. No JS thread blocking. |
| T-05-02 | Information Disclosure | CSS classes reveal implementation details | accept | CSS class names (.reveal, .reveal-stagger) are non-sensitive. No PII or secrets involved. Public static site. |
</threat_model>

<verification>
1. `npm test` — all phase5-animations tests pass
2. `npm run build` — Astro build succeeds with no errors
3. Manual: scroll through dev server, verify each section reveals with fade-up
4. Manual: enable prefers-reduced-motion, verify all content visible immediately
5. Manual: open in Firefox, verify all content visible (no animation, no hidden content)
</verification>

<success_criteria>
- All non-hero sections (.astro components) have .reveal or .reveal-stagger classes
- global.css contains complete @keyframes + @supports + fallback + reduced-motion blocks
- Zero new JavaScript added (no new npm packages, no new .ts/.tsx files for animation)
- Build passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-animations-deploy/05-01-SUMMARY.md`
</output>
