---
phase: 07-discoverability-social-presence
plan: 05
type: execute
wave: 2
depends_on: [07-02, 07-03, 07-04]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements:
  - SEO-07
  - OG-06
must_haves:
  truths:
    - "Homepage passes explicit title, description, and ogImageSlug='home' to BaseLayout"
    - "Built homepage (dist/index.html) references /og/home.png as og:image (absolute URL)"
    - "Full build + vitest + playwright gate is green"
    - "User has visually verified /now and /og/home.png + /og/now.png in a browser"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage wiring for per-page OG image and explicit meta"
      contains: 'ogImageSlug="home"'
  key_links:
    - from: "src/pages/index.astro"
      to: "/og/home.png"
      via: "ogImageSlug prop -> Head.astro -> og:image meta"
      pattern: 'ogImageSlug=["\\x27]home["\\x27]'
---

<objective>
Wire the homepage to use the new per-page OG image system, then run the full Phase 7 verification gate (build + vitest + playwright) against a production preview server, and hand off to the user for visual sign-off on `/now`, `/og/home.png`, and `/og/now.png`.

Purpose: This is the final integration and UAT plan. Plans 02/03/04 landed the pieces independently in Wave 1; Plan 05 stitches them together at the homepage and asks the user to visually confirm the result before phase completion.

Output: Updated `index.astro` with explicit meta + `ogImageSlug="home"`, a full green test gate, and a user-approved phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-discoverability-social-presence-make-the-site-discoverable-a/07-UI-SPEC.md
@.planning/phases/07-discoverability-social-presence-make-the-site-discoverable-a/07-VALIDATION.md
@src/pages/index.astro
@src/components/Head.astro
@src/layouts/BaseLayout.astro
@tests/phase7-seo.test.ts
@tests/phase7-nav.test.ts
@tests/phase7-analytics.test.ts
@tests/phase7-og.test.ts
@tests/phase7-build.test.ts
@tests/phase7.spec.ts

<interfaces>
Current `src/pages/index.astro` (verbatim, 16 lines):

```astro
--- 
import BaseLayout from "../layouts/BaseLayout.astro";
import HeroSection from "../components/HeroSection.tsx";
import AboutSection from "../components/AboutSection.astro";
import CVSection from "../components/CVSection.astro";
import ProjectsSection from "../components/ProjectsSection.astro";
import ContactSection from "../components/ContactSection.astro";
--- 

<BaseLayout title="Dragos Macsim -- AI Data Specialist">
  <HeroSection client:load />
  <AboutSection />
  <CVSection />
  <ProjectsSection />
  <ContactSection />
</BaseLayout>
```

After Plan 02 lands, `BaseLayout` accepts `description`, `ogImageSlug`, `ogImageAlt` - all optional. After Plan 04 lands, `/og/home.png` is a valid build output.

Homepage copy locked by UI-SPEC Copywriting Contract (line 347):
- description: "Dragos Macsim - AI data specialist. Explore my work, experience, and download my CV." (matches existing default in Head.astro - passing it explicitly makes it immune to default changes)
- og slug: "home"
- og alt: "Dragos Macsim - AI Specialist & Product Builder" (matches Head.astro default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire index.astro to use ogImageSlug='home' and explicit description</name>
  <read_first>
    - src/pages/index.astro
    - src/components/Head.astro (confirm ogImageSlug prop from Plan 02)
    - src/layouts/BaseLayout.astro (confirm Plan 03 forwards ogImageSlug)
    - .planning/phases/07-discoverability-social-presence-make-the-site-discoverable-a/07-UI-SPEC.md (Copywriting Contract lines 331-350)
  </read_first>
  <action>
Replace the `<BaseLayout ...>` opening tag in `src/pages/index.astro` with:

```astro
<BaseLayout
  title="Dragos Macsim -- AI Data Specialist"
  description="Dragos Macsim - AI data specialist. Explore my work, experience, and download my CV."
  ogImageSlug="home"
  ogImageAlt="Dragos Macsim - AI Specialist & Product Builder"
>
```

Keep the title string exactly as it was (`"Dragos Macsim -- AI Data Specialist"` with two hyphens - this is the existing value and matches SEO test expectations). The rest of the file (imports, child components, closing `</BaseLayout>`) is UNCHANGED.

Rationale:
- Explicit `description=` makes the homepage immune to future changes to the default in `Head.astro`. Value matches the existing default, so meta description output is unchanged.
- `ogImageSlug="home"` triggers Head.astro to build `og:image` -> `https://dragosmacsim.com/og/home.png` (absolute, per Head.astro `new URL(ogImagePath, Astro.site).toString()`).
- `ogImageAlt` sets the `og:image:alt` meta tag.

After writing, run the full phase gate command:

```
npm run build && npm test -- --run
```

Both must exit 0. Expected state:
- `dist/index.html` now contains `<meta property="og:image" content="https://dragosmacsim.com/og/home.png" />`
- `dist/now/index.html` contains `<meta property="og:image" content="https://dragosmacsim.com/og/now.png" />`
- `dist/sitemap-index.xml` and `dist/sitemap-0.xml` exist, `sitemap-0.xml` contains both `https://dragosmacsim.com/` and `https://dragosmacsim.com/now/`
- `dist/og/home.png` and `dist/og/now.png` exist, > 5KB, valid PNGs
- `dist/robots.txt` exists with the sitemap pointer
- All 6 vitest files green (phase7-seo, phase7-nav, phase7-analytics, phase7-og, phase7-build, plus the existing seo-metadata)

Then start the preview server in the background:

```
npm run preview &
```

Wait ~2 seconds for it to bind to `http://localhost:4321`, then run the Playwright suite against the PROD build:

```
npm run test:e2e -- tests/phase7.spec.ts
```

All three tests should pass: NOW-01 (/now renders), OG-05 home endpoint (200 + PNG), and the unknown-slug 404. Kill the preview process after.

If the Playwright `NOW-01` test fails because the h1 text does not match, double-check `src/pages/now.astro` contains literal "What I'm doing now" (UI-SPEC line 166). If `OG-05` fails with a 500, check the build log for Satori errors (most likely a wrong font path).
  </action>
  <verify>
    <automated>grep -q 'ogImageSlug="home"' src/pages/index.astro && grep -q 'description="Dragos Macsim - AI data specialist' src/pages/index.astro && npm run build && test -f dist/og/home.png && test -f dist/og/now.png && test -f dist/robots.txt && test -f dist/sitemap-index.xml && grep -q 'dragosmacsim.com/og/home.png' dist/index.html && grep -q 'dragosmacsim.com/og/now.png' dist/now/index.html && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/index.astro` contains `ogImageSlug="home"`
    - `src/pages/index.astro` contains explicit `description=` prop
    - `src/pages/index.astro` contains `ogImageAlt=` prop
    - `src/pages/index.astro` still has the same five child components (HeroSection, AboutSection, CVSection, ProjectsSection, ContactSection)
    - `src/pages/index.astro` title is unchanged (`"Dragos Macsim -- AI Data Specialist"`)
    - `npm run build` exits 0
    - `dist/index.html` exists and contains `og:image` pointing at `https://dragosmacsim.com/og/home.png`
    - `dist/now/index.html` exists and contains `og:image` pointing at `https://dragosmacsim.com/og/now.png`
    - `dist/og/home.png` and `dist/og/now.png` both exist and are valid PNGs > 5KB
    - `dist/robots.txt` and `dist/sitemap-index.xml` both exist
    - `dist/sitemap-0.xml` contains both `/` and `/now` URLs
    - `npm test -- --run` exits 0 with every phase7 test file green
    - `tests/phase7.spec.ts` Playwright suite passes against `npm run preview`
  </acceptance_criteria>
  <done>Homepage wired to the new OG system; full build green; full vitest+playwright gate green; production artifacts verified on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User visual verification of Phase 7 surfaces</name>
  <what-built>
All Phase 7 surfaces are now live in a production build:

1. **/now page** with current-focus content (UI-SPEC Surface 1)
2. **Nav entry** for Now on both desktop and mobile, with active state when on /now
3. **Cloudflare Web Analytics beacon** injected only on PROD builds (guarded by `import.meta.env.PROD`)
4. **Branded OG images** at `/og/home.png` and `/og/now.png` (1200x630, dark background, Inter 700 title + Inter 400 subtitle)
5. **Extended JSON-LD Person schema** with image, email, description, knowsAbout
6. **robots.txt** pointing to sitemap-index.xml
7. **Per-page sitemap** at `sitemap-index.xml` + `sitemap-0.xml`
  </what-built>
  <how-to-verify>
Start the production preview server:
```
npm run preview
```
(Binds to http://localhost:4321 - preview serves the built dist/, so PROD-guarded code runs.)

Then perform each check:

**Check 1: Homepage unchanged (regression)**
- Visit http://localhost:4321/
- Confirm hero, about, CV, projects, contact sections still render normally
- Click nav links (#about, #experience, #projects, #contact) - each should smooth-scroll to its section
- Expected: no visual regressions from Phase 6

**Check 2: Now nav link (desktop)**
- At viewport >= 768px, confirm a new "Now" link appears in the nav as the 5th/last link (after Contact)
- Hover it: colour changes to white (`--color-text`)
- Click it: the browser navigates to `/now` (no smooth-scroll intercept)
- Expected: full page navigation

**Check 3: /now page renders**
- On /now, confirm you see:
  - h1 "What I'm doing now" at the display font size
  - "Last updated: April 2026" muted timestamp
  - Intro paragraph about mytai / MSc / Mindrift
  - h2 "Working on" + 3 bullet items
  - h2 "Learning" + 2 bullet items
  - h2 "Location" + "Based in London."
  - divider + footer "This is a /now page, an idea by Derek Sivers." with `/now page` as a link to nownownow.com
- Confirm the nav Now link now shows active (indigo colour + underline)
- Toggle theme: confirm /now responds to light/dark correctly
- Scroll back up: confirm the `.reveal` animation triggered

**Check 4: Now nav link (mobile)**
- Resize browser to < 768px
- Open hamburger menu
- Confirm "Now" is the 5th/last item in the mobile list
- Tap it: full-page navigation to /now
- Expected: menu closes, route changes

**Check 5: OG images render**
- Visit http://localhost:4321/og/home.png
- Confirm a PNG downloads/displays showing:
  - Dark background
  - Indigo "dragosmacsim.com" label at top
  - Large white "Dragos Macsim" title
  - Muted "AI Specialist & Product Builder" subtitle
- Visit http://localhost:4321/og/now.png
- Confirm similar layout with "What I'm doing now" / "Dragos Macsim" copy
- Visit http://localhost:4321/og/unknown.png - confirm 404

**Check 6: Meta tags (view source)**
- On /, view source and confirm:
  - `<meta property="og:image" content="https://dragosmacsim.com/og/home.png" />`
  - `<meta property="og:image:alt" content="Dragos Macsim - AI Specialist & Product Builder" />`
  - JSON-LD script contains `"knowsAbout"`, `"email"`, `"image"`, `"description"` fields
- On /now, view source and confirm:
  - `<meta property="og:image" content="https://dragosmacsim.com/og/now.png" />`
  - `<meta property="og:image:alt" content="What I'm doing now - Dragos Macsim" />`

**Check 7: robots + sitemap**
- Visit http://localhost:4321/robots.txt - confirm 5-line content with sitemap pointer
- Visit http://localhost:4321/sitemap-index.xml - confirm XML index
- Visit http://localhost:4321/sitemap-0.xml - confirm both `/` and `/now` URLs listed

**Check 8: CF beacon is NOT in dev**
- Run `npm run dev` (separate terminal)
- View source on http://localhost:4321/
- Confirm NO `cloudflareinsights.com` script is present (dev mode is guarded)
- Kill `npm run dev`

**Check 9: CF beacon IS in prod build (requires token)**
- If you have set `PUBLIC_CF_ANALYTICS_TOKEN` in .env, run `npm run build && npm run preview`
- View source on http://localhost:4321/
- Confirm a `<script defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token": "..."}'>` tag is present
- If the token is unset, the beacon is correctly absent (fail-closed per Plan 03 design)

**Check 10: LinkedIn preview (optional, requires deploy)**
- After merging and deploying to dragosmacsim.com, open LinkedIn's Post Inspector https://www.linkedin.com/post-inspector/
- Paste https://dragosmacsim.com/ and confirm the preview shows the new branded home OG card
- Paste https://dragosmacsim.com/now and confirm the /now OG card
- This check is deferred to post-deploy; do not block phase completion on it.
  </how-to-verify>
  <resume-signal>Type "approved" if everything renders correctly, or describe specific issues (e.g. "OG font looks wrong", "/now h1 missing", "nav link placement off") so the planner can spawn a gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time static output -> production CDN | All artifacts verified on disk before deploy |
| User browser -> production beacon | Only populated when PUBLIC_CF_ANALYTICS_TOKEN is set at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering/XSS | Homepage meta string values (title, description, alt) | mitigate | All strings are hand-authored literals in index.astro; Astro auto-escapes `{expr}` in attributes. |
| T-7-06 | Info Disclosure | `/og/<slug>.png` URL emitted in meta | accept | Slug `"home"` is allowlisted in Plan 04's `PAGES` array; verified by Plan 04's test. |
</threat_model>

<verification>
- `npm run build && npm test -- --run` both exit 0
- All six phase7 test files green
- `tests/phase7.spec.ts` Playwright suite green against `npm run preview`
- User has approved via checkpoint Task 2
- Every `dist/` artifact listed in acceptance criteria for Task 1 exists
</verification>

<success_criteria>
1. Homepage index.astro passes explicit description + ogImageSlug='home' + ogImageAlt
2. Built homepage meta tags reference /og/home.png; /now meta references /og/now.png
3. Full test gate (build + vitest + playwright) is green
4. User has visually verified all 10 checks in Task 2 and typed "approved"
5. Phase 7 is ready for `/gsd-verify-work` and commit
</success_criteria>

<output>
After completion, create `.planning/phases/07-discoverability-social-presence-make-the-site-discoverable-a/07-05-SUMMARY.md`
</output>
