---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Head.astro
  - src/components/ThemeToggle.astro
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - tests/design-system.test.ts
  - tests/theme-toggle.test.ts
autonomous: false
requirements:
  - DSGN-02
  - DSGN-03

must_haves:
  truths:
    - "Dark mode toggle in top-right corner switches theme without page reload"
    - "Theme persists across page refresh via localStorage"
    - "System color-scheme preference is respected on first visit"
    - "No flash of unstyled content (FOUC) on page load"
    - "Layout renders correctly at 320px, 768px, and 1280px viewports"
    - "Page uses BaseLayout with Head component and global.css imported"
  artifacts:
    - path: "src/components/Head.astro"
      provides: "Document head with meta tags and FOUC prevention script"
      contains: "is:inline"
    - path: "src/components/ThemeToggle.astro"
      provides: "Dark/light mode toggle button"
      contains: "theme-toggle"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Root layout wrapping all pages"
      contains: "global.css"
    - path: "src/pages/index.astro"
      provides: "Entry page using BaseLayout"
      contains: "BaseLayout"
    - path: "tests/design-system.test.ts"
      provides: "Design token verification tests"
      contains: "describe"
    - path: "tests/theme-toggle.test.ts"
      provides: "Theme toggle behavior tests"
      contains: "describe"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "import statement"
      pattern: "import.*global.css"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Head.astro"
      via: "component import"
      pattern: "import Head"
    - from: "src/components/Head.astro"
      to: "localStorage"
      via: "is:inline FOUC script"
      pattern: "localStorage.getItem"
    - from: "src/components/ThemeToggle.astro"
      to: "document.documentElement"
      via: "classList.toggle"
      pattern: "classList.toggle"
---

<objective>
Build the layout shell, Head component with FOUC prevention, ThemeToggle component, and the index page. Then verify the complete design system works end-to-end with automated tests and a visual checkpoint.

Purpose: This plan delivers the interactive foundation. A working page with the design system applied, dark/light mode toggle, and responsive layout. After this plan, the site is visually functional and all subsequent phases add content into this shell.
Output: Working index page with BaseLayout, Head, ThemeToggle, responsive layout, and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
From Plan 01, tokens defined in src/styles/global.css:
CSS custom properties available as Tailwind utilities:
- Colors: bg-base, bg-surface, text-text, text-text-muted, border-border, text-accent-primary, bg-accent-secondary
- Font: font-sans (Inter Variable)
- Spacing: all standard Tailwind spacing utilities (p-4, gap-8, etc.) from spacing 0.25rem
- Breakpoints: sm: (640px), md: (768px), lg: (1280px)
- Font sizes: Custom properties font-size-label, font-size-body, font-size-heading, font-size-display
- Dark variant: dark: prefix via custom-variant

Light mode activated by .light class on html, dark by .dark class.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Head, BaseLayout, ThemeToggle components and wire index page</name>
  <files>src/components/Head.astro, src/components/ThemeToggle.astro, src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <read_first>
    - src/styles/global.css (current design tokens, verify token names before using them)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Dark Mode Toggle Component section, Theme Transition section, Responsive Layout section, Copywriting Contract section, Hero Visual Anchor section, Implementation Notes section)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 FOUC script, Pattern 3 ThemeToggle, Pattern 4 BaseLayout)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-06, D-07, D-09, D-10)
  </read_first>
  <action>
Create four Astro components. Each .astro file uses Astro's frontmatter fence (three dashes on a line by itself to open and close the script section). All file contents specified exactly below.

FILE 1: src/components/Head.astro

Astro frontmatter section containing:
- Props interface with title (string, required) and description (string, optional)
- Destructure Astro.props with default description: "Dragos Macsim — AI specialist who builds products. Explore my work, experience, and download my CV."

Template section containing:
- meta charset="utf-8"
- meta name="viewport" content="width=device-width, initial-scale=1.0"
- title tag using {title} expression
- meta name="description" using {description} expression
- A script tag with is:inline attribute (CRITICAL: must be is:inline for FOUC prevention) containing this exact JavaScript:

```javascript
(function() {
  var stored = localStorage.getItem("theme");
  if (stored === "light") {
    document.documentElement.classList.add("light");
    document.documentElement.classList.remove("dark");
  } else if (stored === "dark") {
    document.documentElement.classList.add("dark");
    document.documentElement.classList.remove("light");
  } else {
    var systemLight = window.matchMedia("(prefers-color-scheme: light)").matches;
    if (systemLight) {
      document.documentElement.classList.add("light");
      document.documentElement.classList.remove("dark");
    } else {
      document.documentElement.classList.add("dark");
      document.documentElement.classList.remove("light");
    }
  }
})();
```

FOUC logic per D-06: localStorage "light" = light, localStorage "dark" = dark, no stored + system light = light, no stored + system dark = dark, no stored + no pref = dark (default).

FILE 2: src/components/ThemeToggle.astro

Astro frontmatter: empty (just comments about pure Astro, no React needed, D-07).

Template: A button element with these attributes:
- id="theme-toggle"
- type="button"
- aria-label="Switch to light mode"
- class="min-w-[44px] min-h-[44px] flex items-center justify-center rounded-lg text-text-muted hover:text-accent-primary transition-colors duration-200 cursor-pointer"

Inside the button, two SVG icons:
- Sun SVG (id="icon-sun"): class="hidden dark:block w-5 h-5", Heroicons sun outline path: "M12 3v2.25m6.364.386-1.591 1.591M21 12h-2.25m-.386 6.364-1.591-1.591M12 18.75V21m-4.773-4.227-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0Z"
- Moon SVG (id="icon-moon"): class="block dark:hidden w-5 h-5", Heroicons moon outline path: "M21.752 15.002A9.72 9.72 0 0 1 18 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 0 0 3 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 0 0 9.002-5.998Z"

Both SVGs: xmlns="http://www.w3.org/2000/svg", fill="none", viewBox="0 0 24 24", stroke-width="1.5", stroke="currentColor", aria-hidden="true".

Script tag (NOT is:inline, regular Astro script) with this exact logic:

```javascript
const toggle = document.getElementById("theme-toggle");

function updateAriaLabel() {
  const isDark = document.documentElement.classList.contains("dark");
  toggle?.setAttribute("aria-label", isDark ? "Switch to light mode" : "Switch to dark mode");
}

toggle?.addEventListener("click", () => {
  const isDark = document.documentElement.classList.contains("dark");
  if (isDark) {
    document.documentElement.classList.remove("dark");
    document.documentElement.classList.add("light");
    localStorage.setItem("theme", "light");
  } else {
    document.documentElement.classList.remove("light");
    document.documentElement.classList.add("dark");
    localStorage.setItem("theme", "dark");
  }
  updateAriaLabel();
});

updateAriaLabel();
```

FILE 3: src/layouts/BaseLayout.astro

Astro frontmatter containing:
- import "@fontsource-variable/inter"
- import "../styles/global.css"
- import Head from "../components/Head.astro"
- import ThemeToggle from "../components/ThemeToggle.astro"
- Props interface: title (string), description (string, optional)
- Destructure Astro.props

Template: Full HTML document structure:
- doctype html
- html lang="en" class="dark"
- head containing Head component with title and description props
- body class="bg-base text-text min-h-dvh" containing:
  - nav: class="fixed top-0 right-0 left-0 z-50 bg-base/80 backdrop-blur-sm border-b border-border"
    - Inner div: class="max-w-[1100px] mx-auto px-4 md:px-8 lg:px-12 flex items-center justify-end h-14"
    - Contains ThemeToggle component
  - main: class="max-w-[1100px] mx-auto px-4 md:px-8 lg:px-12 pt-20"
    - Contains slot element

Layout notes per UI-SPEC: Mobile px-4 (16px each side), Tablet md:px-8, Desktop lg:px-12 with max-w-[1100px]. pt-20 accounts for fixed nav (h-14 + spacing). Nav uses bg-base/80 + backdrop-blur for glass effect.

FILE 4: src/pages/index.astro

Astro frontmatter: import BaseLayout from "../layouts/BaseLayout.astro"

Template:
- BaseLayout with title="Dragos Macsim — AI Specialist and Product Builder"
- Inside: section class="py-24 flex flex-col items-center justify-center min-h-[60dvh]"
  - h1 class="text-center" containing "Dragos Macsim"
  - p class="text-text-muted mt-4 text-center" with inline style font-size: var(--font-size-body) containing "AI Specialist and Product Builder"

This placeholder demonstrates the design system works: custom font, color tokens, responsive layout, dark/light mode.
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Head.astro contains `script is:inline`
    - src/components/Head.astro contains `localStorage.getItem("theme")`
    - src/components/Head.astro contains `prefers-color-scheme: light`
    - src/components/Head.astro contains `name="viewport"`
    - src/components/ThemeToggle.astro contains `id="theme-toggle"`
    - src/components/ThemeToggle.astro contains `min-w-[44px] min-h-[44px]`
    - src/components/ThemeToggle.astro contains `aria-label=`
    - src/components/ThemeToggle.astro contains `localStorage.setItem("theme"`
    - src/components/ThemeToggle.astro contains `id="icon-sun"` and `id="icon-moon"`
    - src/layouts/BaseLayout.astro contains `import "../styles/global.css"`
    - src/layouts/BaseLayout.astro contains `import "@fontsource-variable/inter"`
    - src/layouts/BaseLayout.astro contains `max-w-[1100px]`
    - src/layouts/BaseLayout.astro contains `class="dark"` on the html element
    - src/layouts/BaseLayout.astro contains `ThemeToggle`
    - src/pages/index.astro contains `import BaseLayout`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All four components created: Head.astro with FOUC-prevention script, ThemeToggle.astro with 44px touch target and sun/moon SVG icons, BaseLayout.astro with responsive layout and fixed nav, and index.astro using BaseLayout. Build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create automated tests for design system and theme toggle</name>
  <files>tests/design-system.test.ts, tests/theme-toggle.test.ts</files>
  <read_first>
    - src/styles/global.css (verify exact token names to assert against)
    - src/components/ThemeToggle.astro (verify toggle logic to test)
    - src/components/Head.astro (verify FOUC script logic)
    - vitest.config.ts (verify test configuration)
    - .planning/phases/01-foundation/01-VALIDATION.md (test map and verification strategy)
  </read_first>
  <behavior>
    - Test: global.css file contains @theme block
    - Test: global.css contains all 9 dark mode color tokens with correct hex values
    - Test: global.css contains .light override block with all 8 light mode color tokens
    - Test: global.css contains all 4 font-size tokens (label, body, heading, display)
    - Test: global.css contains font-sans with "Inter Variable"
    - Test: global.css contains spacing 0.25rem
    - Test: global.css contains 3 breakpoint tokens (sm, md, lg)
    - Test: global.css contains 300ms ease transition rule
    - Test: global.css contains custom-variant dark declaration
    - Test: Head.astro contains is:inline FOUC prevention script
    - Test: ThemeToggle.astro contains theme-toggle button with 44px minimum touch target
    - Test: No tailwind.config.js or tailwind.config.mjs exists
  </behavior>
  <action>
Create two test files that verify the design system is complete by reading source files and asserting content. Static analysis tests that guard against accidental token removal.

FILE 1: tests/design-system.test.ts

Import describe, it, expect from vitest. Import readFileSync, existsSync from node:fs. Import join from node:path.

Read src/styles/global.css into a const at module level. ROOT = join(import.meta.dirname, "..").

describe("Design Token System"):
- it contains @theme block: assert toContain("@theme {")
- it imports tailwindcss: assert toContain('@import "tailwindcss"')
- it imports @fontsource-variable/inter: assert toContain('@import "@fontsource-variable/inter"')
- it declares dark mode custom variant: assert toContain("@custom-variant dark")

describe("Dark mode color tokens (default)"):
For each of these 9 tokens, create a test asserting globalCss toContain the token:value pair:
  - --color-base: #0a0a0f
  - --color-surface: #111118
  - --color-border: #27272a
  - --color-text: #e4e4e7
  - --color-text-muted: #71717a
  - --color-accent-primary: #6366f1
  - --color-accent-primary-hover: #818cf8
  - --color-accent-primary-press: #4f46e5
  - --color-accent-secondary: #f59e0b

describe("Light mode color overrides"):
- Assert globalCss toContain(".light {")
- Extract the .light block via slice from indexOf(".light {") to next "}"
- For each of these 8 tokens, assert lightBlock toContain:
  - --color-base: #fafafa
  - --color-surface: #ffffff
  - --color-border: #e4e4e7
  - --color-text: #18181b
  - --color-accent-primary: #4f46e5
  - --color-accent-primary-hover: #6366f1
  - --color-accent-primary-press: #3730a3
  - --color-accent-secondary: #d97706

describe("Typography tokens"):
- Assert toContain '"Inter Variable"'
- Assert toContain "--font-size-label: 0.875rem"
- Assert toContain "--font-size-body: clamp(1rem, 2vw, 1.125rem)"
- Assert toContain "--font-size-heading: clamp(1.25rem, 3vw, 2.5rem)"
- Assert toContain "--font-size-display: clamp(2.5rem, 5vw, 4rem)"

describe("Spacing and breakpoints"):
- Assert --spacing: 0.25rem, --breakpoint-sm: 40rem, --breakpoint-md: 48rem, --breakpoint-lg: 80rem

describe("Base styles"):
- Assert toContain "transition: background-color 300ms ease, color 300ms ease"
- Assert toContain "line-height: 1.7" and "line-height: 1.6"
- Assert toContain "max-width: 42.5rem"

describe("No legacy config files"):
- Assert existsSync(join(ROOT, "tailwind.config.js")) is false
- Assert existsSync(join(ROOT, "tailwind.config.mjs")) is false

FILE 2: tests/theme-toggle.test.ts

Same imports pattern. Read ThemeToggle.astro, Head.astro, BaseLayout.astro from their paths.

describe("Theme Toggle Component"):
- Assert toggleContent toContain 'id="theme-toggle"'
- Assert min-w-[44px] and min-h-[44px]
- Assert aria-label=
- Assert id="icon-sun" and id="icon-moon"
- Assert localStorage.setItem("theme"
- Assert classList.remove and classList.add
- Assert updateAriaLabel

describe("FOUC Prevention Script"):
- Assert headContent toContain "is:inline"
- Assert localStorage.getItem("theme")
- Assert prefers-color-scheme
- Assert classList.add("dark") and classList.add("light")
- Assert name="viewport" and width=device-width

describe("BaseLayout"):
- Assert layoutContent toContain "global.css"
- Assert @fontsource-variable/inter
- Assert contains Head and ThemeToggle component references
- Assert max-w-[1100px]
- Assert px-4, md:px-8, lg:px-12
- Assert lang="en" and class="dark"

Run: npx vitest run --reporter=verbose
  </action>
  <verify>
    <automated>cd "/Users/dragos/Library/CloudStorage/OneDrive-Personal/Documents/Dragos CV/Personal Page" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/design-system.test.ts exists and contains `describe("Design Token System"`
    - tests/theme-toggle.test.ts exists and contains `describe("Theme Toggle Component"`
    - tests/design-system.test.ts asserts all 9 dark mode color token hex values
    - tests/design-system.test.ts asserts all 8 light mode override hex values
    - tests/design-system.test.ts asserts all 4 font-size tokens
    - tests/design-system.test.ts asserts no tailwind.config.js exists
    - tests/theme-toggle.test.ts asserts is:inline FOUC script
    - tests/theme-toggle.test.ts asserts 44px touch target
    - tests/theme-toggle.test.ts asserts localStorage persistence
    - `npx vitest run` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>All tests pass. Design system tests verify every token value. Theme toggle tests verify FOUC prevention, localStorage, accessibility, and touch targets. BaseLayout tests verify responsive layout and component wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of design system and dark mode toggle</name>
  <files>src/pages/index.astro</files>
  <what-built>
    Complete Phase 1 foundation: Astro project with bespoke design system (custom Inter font, indigo/amber color palette, responsive layout), dark/light mode toggle with FOUC prevention, and automated tests.
  </what-built>
  <action>Human visual verification of the running dev server.</action>
  <how-to-verify>
    1. Start the dev server: npm run dev in the project directory
    2. Open http://localhost:4321 in your browser
    3. Verify dark mode (default): background very dark (#0a0a0f), text light (#e4e4e7), Inter font visible
    4. Click the sun icon in the top-right corner: background transitions smoothly (300ms) to light (#fafafa), text flips to dark (#18181b), icon changes to moon, no page reload
    5. Refresh the page: light mode persists (no FOUC)
    6. Test responsive layout: at 320px width content fills with 16px padding each side, at 768px centered with generous padding, at 1280px constrained to 1100px max-width
    7. Clear localStorage "theme" key and refresh: should default to dark mode (or follow system preference)
  </how-to-verify>
  <verify>Visual inspection by human</verify>
  <done>Human confirms: dark mode default works, theme toggle transitions smoothly, font renders correctly, responsive layout works at all three breakpoints, and no FOUC on refresh.</done>
  <resume-signal>Type "approved" to confirm the design system looks correct, or describe any visual issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage read/write | Theme toggle reads and writes "theme" key, non-sensitive user-controlled preference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | FOUC script reads localStorage | accept | Theme preference is non-sensitive; spoofing it only changes visual appearance for the local user |
| T-01-05 | Tampering | ThemeToggle writes to localStorage | accept | Only writes "dark" or "light" string values; no code execution risk |
| T-01-06 | Information Disclosure | Meta description tag | accept | Contains only public professional information intended for SEO |
</threat_model>

<verification>
1. `npm run build` completes with zero errors
2. `npx vitest run` passes all tests
3. Dev server renders page with Inter font, dark background, light text
4. Theme toggle switches between dark and light without page reload
5. Theme persists across page refresh
6. Layout responsive at 320px, 768px, 1280px viewports
</verification>

<success_criteria>
- Head.astro contains synchronous FOUC-prevention script with is:inline
- ThemeToggle.astro has 44px touch target, sun/moon SVG icons, localStorage persistence
- BaseLayout.astro imports global.css, uses Head and ThemeToggle, has responsive max-w-[1100px] layout
- index.astro uses BaseLayout
- All automated tests pass (design-system.test.ts and theme-toggle.test.ts)
- Human verifies: dark mode default, smooth theme transition, font rendering, responsive layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
