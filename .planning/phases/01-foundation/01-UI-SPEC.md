---
phase: 1
slug: foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-09
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> Phase 1 establishes the design system. All tokens defined here are the source of truth for every subsequent phase.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Tailwind v4 `@theme` — bespoke token system, no shadcn) |
| Preset | not applicable |
| Component library | none (Astro components, zero external component library) |
| Icon library | none in Phase 1 (Heroicons or Lucide added in Phase 2 when toggle icon required) |
| Font | Inter Variable — self-hosted via `@fontsource-variable/inter` |

**Source:** CONTEXT.md D-11, RESEARCH.md Standard Stack. No `components.json` found — greenfield project confirmed.

**shadcn gate:** Stack is Astro 5 + Tailwind v4. shadcn is React/Next.js-oriented and does not align with this Astro-native stack. No initialization required. Registry safety gate: not applicable.

---

## Spacing Scale

Declared values (all multiples of 4). Implemented via Tailwind v4 `--spacing: 0.25rem` base in `@theme` — all spacing utilities (p-4, gap-8, etc.) scale from this 4px unit.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing (e.g. badge padding, icon-label gap) |
| md | 16px | Default element spacing (e.g. nav link padding, list gaps) |
| lg | 24px | Section internal padding |
| xl | 32px | Layout gaps between major elements |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level vertical rhythm between sections |
| 4xl | 96px | Hero top/bottom padding |

Exceptions: Dark mode toggle touch target minimum 44px (per WCAG 2.5.8). Implemented as `min-w-[44px] min-h-[44px]` utility on the toggle button.

**Source:** RESEARCH.md Pattern 1 (`--spacing: 0.25rem` Tailwind v4 base). Scale extended to 96px for hero. CLAUDE.md DSGN-02 (responsive layout requirement).

---

## Breakpoints

| Name | Value | Target |
|------|-------|--------|
| sm | 640px (40rem) | Mobile landscape / small tablet |
| md | 768px (48rem) | Tablet |
| lg | 1280px (80rem) | Desktop |

Mobile-first approach: base styles target 320px minimum, scale up with `sm:`, `md:`, `lg:` prefixes.

**Source:** RESEARCH.md Pattern 1. ROADMAP.md Phase 1 success criterion 4 (320px / 768px / 1280px viewports).

---

## Typography

Inter Variable loaded at weights 400, 600, 700. No other font family. Differentiation via size and weight only.

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Body (desktop) | 18px (1.125rem) | 400 | 1.7 | Max prose width 680px |
| Body (mobile) | 16px (1rem) | 400 | 1.6 | Applied via `@media (max-width: 40rem)` |
| Label / UI text | 14px (0.875rem) | 600 | 1.4 | Nav links, badges, captions |
| H3 | clamp(1.25rem, 2vw, 1.75rem) — approx 20px–28px | 600 | 1.2 | Section subheadings |
| H2 | clamp(1.75rem, 3vw, 2.5rem) — approx 28px–40px | 700 | 1.2 | Section headings |
| H1 / Display | clamp(2.5rem, 5vw, 4rem) — approx 40px–64px | 700 | 1.1 | Hero display — single H1 per page |

Declared sizes: 14px, 16px, 18px, 28px, 40px, 64px (clamped range — 4 semantic tiers: label, body, heading, display).
Declared weights: 400 (regular) + 600 (semibold) + 700 (bold) — three weights, justified by editorial hierarchy requirement (CONTEXT.md D-12).

**Source:** CONTEXT.md D-11, D-12, D-13. RESEARCH.md Pattern 1 font-size tokens. Heading clamp values from RESEARCH.md verified CSS example.

---

## Color

### Dark Mode (default)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #0a0a0f | Page background (`--color-base`) |
| Secondary (30%) | #111118 | Card surfaces, nav background (`--color-surface`) |
| Border | #27272a | All borders, dividers (`--color-border`) |
| Text primary | #e4e4e7 | All primary text (`--color-text`) |
| Text muted | #71717a | Secondary text, captions, placeholders (`--color-text-muted`) |
| Accent primary | #6366f1 | Nav links, focus rings, active states (`--color-accent-primary`) |
| Accent primary hover | #818cf8 | Hover state on accent primary elements (`--color-accent-primary-hover`) |
| Accent primary pressed | #4f46e5 | Active/pressed state (`--color-accent-primary-press`) |
| Accent secondary | #f59e0b | CTAs, badges, highlight marks (`--color-accent-secondary`) |

### Light Mode overrides

| Role | Value | CSS Token |
|------|-------|-----------|
| Dominant (60%) | #fafafa | `--color-base` |
| Secondary (30%) | #ffffff | `--color-surface` |
| Border | #e4e4e7 | `--color-border` |
| Text primary | #18181b | `--color-text` |
| Text muted | #71717a | `--color-text-muted` (same in both modes) |
| Accent primary | #4f46e5 | `--color-accent-primary` (darkened for contrast) |
| Accent primary hover | #6366f1 | `--color-accent-primary-hover` |
| Accent primary pressed | #3730a3 | `--color-accent-primary-press` |
| Accent secondary | #d97706 | `--color-accent-secondary` (darkened for contrast) |

### Accent Reserved-For List

Accent primary (#6366f1 dark / #4f46e5 light) is reserved for:
- Navigation link active/current state
- Focus rings on interactive elements (keyboard navigation outline)
- Inline hyperlinks
- ThemeToggle hover state

Accent secondary (#f59e0b dark / #d97706 light) is reserved for:
- Primary CTA button (download CV, contact)
- Skill/technology badge backgrounds
- Decorative highlight marks on key phrases

Accent colors are NOT used on: body text, backgrounds, borders, or decorative dividers.

**Source:** CONTEXT.md D-01, D-02, D-03, D-08. RESEARCH.md Pattern 1 verified CSS token values.

---

## Theme Transition

| Property | Value |
|----------|-------|
| Duration | 300ms |
| Easing | ease |
| Properties | background-color, color |
| Applied on | `html` element via CSS transition |
| FOUC prevention | `is:inline` script in `<head>` reads localStorage, sets `.dark`/`.light` class on `<html>` synchronously before first paint |

Default theme: dark. System preference respected on first visit. localStorage key: `"theme"`, values: `"dark"` | `"light"`.

**Source:** CONTEXT.md D-06, D-07, D-09. RESEARCH.md Pattern 2 (FOUC script).

---

## Gradient Glow Effects

Subtle radial gradient overlays only. No glassmorphism, no heavy decoration.

| Usage | Dark Mode | Light Mode |
|-------|-----------|------------|
| Hero background glow | Soft indigo radial gradient at 15–20% opacity | Soft indigo radial gradient at 8–10% opacity (pastel) |
| Accent element glow | Amber/indigo box-shadow glow at 20% opacity | Amber/indigo box-shadow glow at 12% opacity |

Glows implemented as CSS `radial-gradient` or `box-shadow` utility — no SVG filters, no canvas.

**Source:** CONTEXT.md D-05, D-10.

---

## Dark Mode Toggle Component

| Property | Spec |
|----------|------|
| Placement | Top-right navigation corner |
| Icon | Sun icon (light mode active) / Moon icon (dark mode active) |
| Touch target | 44px × 44px minimum |
| Implementation | Pure Astro component (`ThemeToggle.astro`) — no React island |
| Behavior | Toggles `.dark`/`.light` class on `document.documentElement`; writes to localStorage |
| Transition | Inherits 300ms ease from `html` transition |

**Source:** CONTEXT.md D-07. RESEARCH.md Architecture Pattern — `ThemeToggle.astro`.

---

## Responsive Layout Foundations

| Breakpoint | Max content width | Layout notes |
|------------|-------------------|--------------|
| Mobile (320px+) | 100% − 32px padding (16px each side) | Single column, stacked |
| Tablet (768px+) | 720px | Single column, more generous padding |
| Desktop (1280px+) | 1100px | Content constrained, whitespace increases |

Prose (body text) max-width: 680px. Applied to paragraphs and text-heavy sections.
Page wrapper: `max-w-[1100px] mx-auto px-4 md:px-8 lg:px-12`.

**Source:** CONTEXT.md D-04 (generous whitespace). ROADMAP.md Phase 1 success criterion 4.

---

## Copywriting Contract

Phase 1 is a scaffold and design system phase — no user-facing content is delivered yet. The following defines the copy for the one interactive element created in this phase.

| Element | Copy |
|---------|------|
| Dark mode toggle aria-label (dark active) | "Switch to light mode" |
| Dark mode toggle aria-label (light active) | "Switch to dark mode" |
| Page `<title>` (placeholder) | "Dragos Macsim — AI Specialist & Product Builder" |
| Meta description (placeholder) | "Dragos Macsim — AI specialist who builds products. Explore my work, experience, and download my CV." |
| Empty state | not applicable — Phase 1 has no data display |
| Error state | not applicable — Phase 1 has no form or async operation |
| Destructive actions | none in Phase 1 |
| Primary CTA (placeholder for Phase 2) | "Download CV" (amber button, established here as the amber accent CTA pattern) |

**Source:** RESEARCH.md Pattern 2 (Head.astro default description). No content requirements in DSGN-01/02/03.

---

## Implementation Notes for Executor

These are prescriptive implementation contracts, not suggestions.

### Token File Location
All tokens defined in `src/styles/global.css` inside a single `@theme {}` block. No `tailwind.config.js` file. Light mode overrides in a `.light` selector below `@theme`.

### Font Loading
Use `@fontsource-variable/inter`. Import in `src/styles/global.css`:
```css
@import "@fontsource-variable/inter";
```
Font stack: `"Inter Variable", ui-sans-serif, system-ui, sans-serif`.

### Dark Mode Class Strategy
Use `.dark` class on `<html>` (not `data-theme` attribute). Tailwind v4 `@custom-variant`:
```css
@custom-variant dark (&:where(.dark, .dark *));
```

### Transition Rule
Apply on `html` element only:
```css
html {
  transition: background-color 300ms ease, color 300ms ease;
}
```
Do NOT apply `transition: all` — only transitioning background-color and color prevents layout shifts.

### No JavaScript for Static Sections
ThemeToggle is the only JavaScript in Phase 1 (pure Astro `is:inline` + a small module script). No React island required until Phase 2+.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| third-party | none | not applicable |

No external component registries. All Phase 1 components are hand-authored Astro components.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
